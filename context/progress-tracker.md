# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---
- **2026-09-07 — Fixed mobile bottom-sheet entrance: sheets popped in on first open instead of sliding up (user report: "portrait phone <640px, I saw a bottom sheet that just pops in").** Root cause: the open effect flipped `entered` via a single `requestAnimationFrame` whose callback React could batch with `setMounted(true)` into one commit → the browser never painted the `translate3d(0,100%,0)` starting frame → the 450ms CSS transition had no "before" frame → the sheet popped in. Fix in `components/ui/CssBottomSheet.tsx`: removed the racy rAF flip; open effect now only `setMounted(true)`/`setSnapIndex(TOP_INDEX)`/`setSheetOffset(0)`, and a **post-paint entrance effect (deps `[mounted]`) runs deterministically after the off-screen frame is painted** — `void sheetRef.current?.offsetHeight` (forced layout) then `setEntered(true)` (+ eslint-disable comment, same convention as the existing line-120 one; a `useLayoutEffect` variant tripped `react-hooks/set-state-in-effect`, so regular `useEffect`). Also added `overscroll-y-contain` to the root className; drag/handoff/snap logic untouched. **Verified live via Chrome DevTools on the real dev app (390×844 touch, New Event sheet):** entrance transform sampled `452→407→195→27→0` over ~450ms with the first frame fully off-screen — slide-up runs on first open. Drag re-proven with synthetic pointers (setPointerCapture no-op'd): follow-finger 1:1 (`12→100→200`), slow 180px/700ms drag settles to exactly the 50% snap (`226 = 452×0.5`), and a fast 200px/70ms drag (>0.8 px/ms fling) dismisses via `onClose` — all three behaviors correct; the earlier "settle to 0" scare was my own synthetic test flinging the sheet (2.77 px/ms) and dismissing it, which is the fling-dismiss working, not a bug. All checks green: `tsc --noEmit`, `eslint components/ui/CssBottomSheet.tsx`, `scripts/check-bottom-sheet-drag.ts`, `next build` (27 routes). Deploy pending.
- **2026-09-07 — Auto-build re-verified manually.** Pushed this doc-only commit to confirm the GitHub→Vercel pipeline fires without any CLI involvement; a fresh `-git-master-` production deploy appeared and auto-aliased `liquifi-app.vercel.app`.
- **2026-09-07 — Vercel clean-slate + GitHub-owned deploys (user request: delete `liquifi-app`, reconnect GitHub repo as `liquifi-app` at `liquifi-app.vercel.app`, and make the project always track GitHub).** **Infra change — new deploy discipline from here on:** `commit → git push origin master → Vercel auto-deploys`, no more manual `vercel --prod` + `vercel alias set` dance (the old project's `liquifi-app.vercel.app` was a **manual alias** that did NOT auto-track production — that foot-gun is gone by construction). Steps: (1) pushed the 7 previously-unpushed commits (`3b2ec9e`…`6e83025` — bottom-sheet snap port + docs; `git log origin/master..master` now empty), (2) `vercel remove liquifi-app --yes` + `vercel remove liquifi --yes` (deletes projects, their deployments, **manual alias, and all env vars**), (3) deleted stale local `.vercel/` link, (4) `vercel link --yes --project liquifi-app` — created the new project `prj_nLHPFz7Y4TaDWOGiJ20SnARerBvA` **and auto-connected the GitHub repo** `dranrebwen424/Liquifi` (App was already authorized), (5) re-added all env vars from `.env.local` to the new project (19/19 OK — InsForge, Gemini, OpenRouter, Web Push, Polygon, SMTP, `NEXT_PUBLIC_*`, `CRON_SECRET`, `VERCEL_OIDC_TOKEN`; the link command refreshed a fresh `VERCEL_OIDC_TOKEN` into `.env.local`), (6) `vercel --prod` → build green in 54s (Next.js 16.2.10, 27 static + dynamic routes listed) → **`▲ Aliased https://liquifi-app.vercel.app`** — because the project is named exactly `liquifi-app`, that's its **default production domain**, auto-tracking every future push (no manual alias to maintain). Domain was confirmed available the moment the delete freed it (user instruction: if `liquifi-app.vercel.app` had NOT been available, stop and ask before letting Vercel auto-generate — it was available, no detour). Smoke: `liquifi-app.vercel.app` HTTP 200 (`<title>Liquifi</title>`, 31KB), `/login` 200, `/signup` 200; `vercel project ls` shows exactly one new `liquifi-app` (stale `liquifi` gone); alias ls confirms `liquifi-rcijj98oz… → liquifi-app.vercel.app` (default domain, not manual). Production branch = `master` = repo default. Emergency hotfix still possible via local `vercel --prod` since `.vercel/` is re-linked.
- **2026-09-07 — Ported the reference bottom-sheet motion into the shared `CssBottomSheet` (user request: match the reference motion from the mobile bottom sheet spec).** All 8 sheets now use 3-position snap settling + velocity fling + dismiss-at-lowest-snap (`lib/bottom-sheet-drag.ts` — Phase 1–4 complete, **live-drag verified**). `SNAPS=[0.15,0.5,0.92]` (`TOP_INDEX=2`): on release the sheet settles to the nearest snap (180ms/260ms away, 450ms else); fling up `< -0.8px/ms` jumps to the top snap, fling down `> 0.8px/ms` dismisses from anywhere; releasing at the lowest snap dismisses via a new `onClose` prop threaded through all 8 consumers (admin `DepartmentsListClient` → `closeCreate`, `NewEventModal` → `closeSheet`, `EditBudgetModal` → `onClose`, `ArchiveEventModal`/`VoidEntryModal` → guarded `if (!busy)`, `ReceiptReview` → `if (!confirming)`, `EntryDetailModal` → `onClose`, `LogEntryModal` → `if (!confirming && !discarding) closeModal()`). Snap offsets derive from the **measured sheet height**: `translate(TOP)=0`, else `−(h·(1−SNAPS[i]))`, clamped `[−24, h||320]`; ResizeObserver tracks height (341px for LogEntryModal). Top snap rests flush at translate 0 so the scroll-handoff condition (`offsetRef===0`) stays exact; scroll handoff defers to the first `overflow-y` ancestor with overflow while at rest. Dots rail (`right-3 top-3 z-10`, smallest-to-largest = top→mid→close, `aria-pressed`) jumps between snaps. **Trickiest bug (Phase 4, found + fixed):** the sheet-height measure effect had `[]` deps and early-returned while `sheetRef.current` was null (sheet renders only when `mounted`), so `sheetHeight` stayed 0 → every `snapTranslate` = 0 → `resolveSnapIndex` kept index 0 → **every drag closed the sheet**. `[open]` also failed (runs before the mount effect's `setMounted(true)` commits). Final fix: deps `[mounted]` (mounted flips → sheet mounts next render → effect re-runs with the sheet in the DOM). Verified via Chrome DevTools on the real event page (390×844 touch emulation, hard-reload + fresh lazy chunk each retest): slow down-drag 132px @ 0.55px/ms → settles MID `translate3d(0px, 170.5px, 0px)` mounted; slow up-drag 88px → settles TOP `translate3d(0px, 0px, 0px)` mounted; fast down-fling 88px in 40ms (2.2px/ms) → dismisses (unmounted). Sheet left untouched stays open; close is strictly drag/rail-driven. Served chunk `_1197l_n._.js` confirmed carrying the fix. `npx tsx scripts/check-bottom-sheet-drag.ts` + `npx tsc --noEmit` green. **Not yet committed/deployed** — commit + `vercel --prod` + `vercel alias set ... liquifi-app.vercel.app` pending.
- **2026-09-07 — Deployed native whole-sheet drag to `liquifi-app.vercel.app`.** Commit `c0c2b8b`. `vercel --prod` → `liquifi-g98vp4uzg-diamondblack488-7599s-projects.vercel.app` (Ready 55s, auto-aliased `liquifi-blond`), then `vercel alias set liquifi-g98vp4uzg… liquifi-app.vercel.app` → `Success!` re-pointed the manual alias. Smoke: `/login` HTTP 200 + full Liquifi HTML; `liquifi-app.vercel.app` and `liquifi-blond.vercel.app` byte-identical (same build served). Note: the InsForge `create-deployment` proxy path ERROR'd this time (`deployments.runs` status ERROR for `dpl_HPUsn7Xjx4HKjA9QFjVGowme2JdN`, its `insforge.site` URL 404'd throughout) — Vercel CLI `--prod` + manual `vercel alias set` remains the reliable route.
- **2026-09-07 — Native whole-sheet drag replaces the Framer-based grip drag on all 8 `CssBottomSheet` sheets (drag anywhere, snap back, never dismisses).** Deleted `lib/use-drag-to-dismiss.ts` entirely; `CssBottomSheet` now owns the drag inline with raw Pointer Events. `pointerdown` records pointer + start/last Y + deferred scroll target (first `overflow-y:auto/scroll` ancestor with overflow); travel >8px engages the drag (`cursor-grabbing`, transition disabled, `setPointerCapture`) and the sheet follows the finger 1:1; over a scrollable at offset 0, upward drags hand off to inner scroll instead of moving the sheet; `onPointerUp` / `onPointerCancel` / `onLostPointerCapture` all reset to offset `0` — no dismiss threshold, a drag can never close a sheet. Offset state is rAF-coalesced (`setSheetOffset`: writes `offsetRef` per event, one `requestAnimationFrame` per frame so release inside the same frame snaps from the last painted position) and clamped by new `lib/bottom-sheet-drag.ts` (`Math.max(-24, Math.min(delta, 320))`). Sheet markup: `touch-none select-none cursor-grab`, `cursor-grabbing transition-none` while dragging, inline `translate3d(0, offset, 0)` when entered. `NewEventModal`/`LogEntryModal` lost their per-sheet drag hook imports. **Lint:** resolved the one new `react-hooks/set-state-in-effect` error at `CssBottomSheet.tsx:54` (`setMounted(true)` — mount-then-animate double-rAF is the canonical exception; targeted disable comment) and the `exhaustive-deps` warning (`resetSheet` wrapped in `useCallback([])`, deps `[open, resetSheet]`). Remaining scoped lint error `LogEntryModal:61` is pre-existing (present in HEAD, untouched). **Verified:** `eslint` scoped clean on the refactor, `tsc --noEmit` green, `next build` green, deterministic `scripts/check-bottom-sheet-drag.ts` passes, `git diff --check` clean; live (emulated 390×844 touch) drag through the New Event sheet recorded real `pointerdown→pointermove(298px)→pointerup`, zero console errors, snap-back transform confirmed — the one rAF-coalesced intermediate frame is unobservable in a same-task CDP drag by design.

---
- **2026-09-07 — Fix: `FadeIn` emitted a lingering `transform: translateY(0)` (identity but non-none) once visible, which established a fixed-position containing block on the wrapper. Any `position: fixed` modal rendered inside a `FadeIn` (e.g. the View Report "No report yet" dialog in `ViewReportPill` on `app/adviser/events/[eventId]/page.tsx`) was then positioned against the wrapper's box instead of the viewport — it appeared at the top of the page, behind the budget overview card, sized to the header only. Fix (commit `fde204e`): `useFadeIn` now returns `transform: "none"` (not `translateY(0)`) once `visible` — `none` is not a containing block, so fixed overlays/dialogs keep full-viewport centering. Verified on live `liquifi-app.vercel.app` (deploy `liquifi-39q2nd8t0`, re-aliased): opened View Report popup on event `06e93f51` — overlay + dialog container now span full viewport (top 0, height = `innerHeight`), card vertically centered; closed via "Got it"; console unchanged (only pre-existing #418 `text` hydration error). `tsc` + `next build` green.

---
- **2026-09-07 — Adviser smooth stagger fade-in entrances (user request: "smooth stagger fade in entrance animation for adviser home, event, and all event page").** Made the entrance read as a real stagger fade, not a single whole-page fade and not a spring (`72dd8dc` had removed springs for the lag fix; this pass kept fixed tweens, lengthened them slightly and widened the stagger): `lib/motion-variants.ts` `staggerContainer` `staggerChildren 0.04 / delayChildren 0.03`, `fadeUpItem` `y: 10` + `0.28s` tween `[0.16,1,0.3,1]`; `EventPageEntrance` now takes `delay` + `className`, `y: 10`, `0.28s`; adviser home shell + notification swap bumped to match (`0.28s`/`0.22s`). Event detail no longer wraps the whole page in one fade — mobile + desktop sections each animate in sequence via `EventPageEntrance delay` (header 0, budget 0.06, banner 0.12, expenses 0.12/0.18; desktop: back 0, header 0.04, banner 0.08, two-col 0.08/0.14, expenses 0.14/0.2). Verified: `tsc` + `next build` green; live on `liquifi-app.vercel.app` (deploy `liquifi-rj9famy9x`, re-aliased): opendevice sampled `/adviser/home` — 4 cards rise `0.00→0.35→0.82→…→1.00` staggered, sequence done ~500ms; `/adviser/events` clean console (24 event links); event detail renders (Black Friday) with only the known pre-existing #418 hydration error on entry-bearing events. Commits `b9b4c8e` (+ docs).

---
- **2026-09-07 — Switch entrance mechanism from framer-motion to React/CSS `useFadeIn`.** Replaced `components/events/EventPageEntrance.tsx` (deleted) with a single generic `components/ui/FadeIn.tsx` (~50 lines): `useFadeIn(delayMs)` hook co-located; delay in milliseconds; reduced-motion guard via `matchMedia` returning `{}`; renders a plain `div` with inline styles. Per-card stagger `delay={30 + index * 80}` ms. Adviser event detail retuned to 100ms/150ms cadence: mobile 0 / 150 / 300 / 450 (locked 510); desktop back 0 / header 100 / banner 200 / two-col locked 360 / expenses locked 460. Converted: `app/adviser/events/[eventId]/page.tsx`, `app/adviser/events/page.tsx`, `app/treasurer/events/[eventId]/page.tsx`, `app/admin/.../events/[eventId]/page.tsx`, `app/treasurer/home/client.tsx` (4 stagger containers + 4 card maps + welcome bar + topSlot; `useReducedMotion` + `framer-motion` import fully removed), `app/treasurer/events/client.tsx` (2 + 2), `components/entries/EntryList.tsx` (2 + 2). `lib/motion-variants.ts` untouched (still used by admin `DepartmentsListClient` + dialog/sheet springs). `tsc` + `next build` green; live on `liquifi-app.vercel.app` (deploy `liquifi-hrnnl19bc`, re-aliased): `/adviser/home`, `/adviser/events`, `/adviser/events/{eventId}` — zero new console errors (only pre-existing #418), all 11 entries visible on Black Friday event detail. Commits `62d8c71`.

---
- **2026-09-06 — Adviser entrance animation polish:** Smoothed entrance motion on adviser home, adviser all-events, and adviser event detail without adding any library. `lib/motion-variants.ts` now uses a smaller fade-up (`y: 8`) plus a softer spring (`stiffness: 88`, `damping: 24`, `mass: 0.8`) and tighter stagger (`35ms`). `EventPageEntrance` now uses the same spring and respects reduced motion; `/adviser/events` is wrapped in it; the adviser home welcome/top-slot and notification-card swap animation use the same softer curve. Verified `npx tsc --noEmit`, `npm run build`, live `/adviser/home`, `/adviser/events`, and `/adviser/events/{eventId}` on `liquifi-app.vercel.app`; committed `a11831e`, deployed `liquifi-5oyt1b8x5`, re-aliased.

---
- **2026-09-06 — View Report pill: scaled down + no-report button now pops a notice instead of dead-404 link (follow-up).** New `components/adviser/ViewReportPill.tsx` (client): with a report → link to `/adviser/reports/{eventId}` at a slimmer size (`rounded-[15px] px-3 py-[7px] text-[11px]`, down from `rounded-[17px] px-4 py-[10px] text-[12px]`); without a report → always-visible muted button (`border-border bg-surface text-text-muted`) that opens a centered dialog — `FileQuestion` icon (warning circle), "No report yet", "available once the treasurer generates the liquidation report", "Got it" + X close (reuses `dialogOverlay`/`dialogContent` motion variants). Replaces the hidden-pill logic from `fb68314` in `app/adviser/events/[eventId]/page.tsx` (now `hasReport={!!latestReport}`). Verified on `liquifi-app.vercel.app` (adviser session): Black Friday (no report) → muted button, click → popup with icon; Testing (pending report) → slim link to the report page. Committed `48eff26`, deployed `liquifi-5w1zb1cpd`, re-aliased `liquifi-app.vercel.app`.

---
- **2026-09-06 — Adviser Home Figma refresh:** Rebuilt `/adviser/home` on the same read-only event-home surface as Treasurer Home: responsive active-event folder grid, archive list/search hierarchy, shared hamburger/search/bell mobile chrome, and no creation action/FAB. Added a top notification spotlight fed by the acting adviser&apos;s 12 most recent `notifications` rows; it maps canonical notification copy/deep links, animates every current item through a two-row list (reduced motion respected), and renders the supplied `public/adviser/Wumpus Hi.json` asset. Added `/adviser/events` for the View all link. `npx tsc --noEmit` and `npm run build` pass.

---
- **2026-09-05 - Receipt parse: killed the storage round-trip — root cause of the still-18s (user: "it still takes 18 seconds, what model are you using?") is NOT the model — `gemini-3.5-flash-lite` is byte-identical in git since 2026-08-02 (the 5-9s "before" era ran the SAME model; what changed was the pipeline).** Diffing the git-original route (`9bbfe1d`, 2026-08-02) against today's: the original parsed in-memory from the multipart buffer straight to Gemini; today's route uploaded the image to storage, inserted a provisional row, then **downloaded the blob back** (`getReceiptBlob`) just to build the data URL — +2-4s of storage round-trips on a cold Hobby function that pushed every request past the 8s timeout, and the client's 900ms backoff + reparse doubled it to ~18s. **Fix** (`app/api/entries/receipt/route.ts` rewritten): feed the already-client-compressed multipart `File` **directly** to Gemini (data URL from `image.arrayBuffer()` — no storage read); Gemini + `requireRole` start concurrently; the blob upload runs after auth (overlaps the model call; still needed for the retry path + `image_url`); on success the row is inserted **straight to `ai_parsed`** (single insert, no provisional hop). Verdict → delete blob + 422 + audit; duplicate → delete blob + 409; transient Gemini failure → insert `pending_ai_parse` (blob already uploaded) + audit, so the `[entryId]` retry route works unchanged. Untouched: `[entryId]` reparse route (still reads the stored blob — correct there), `lib/gemini.ts` (8s timeout kept), `lib/image.ts` (1600/q0.8), `MAX_ATTEMPTS=2`. Critical path is now `formData → [parallel: requireRole + Gemini] → dup check → 1 insert → audit` ≈ 3-4s warm. `tsc --noEmit` clean. Deployed `liquifi-mnca8h3h0` (first `--prod` attempt failed with a transient "Not authorized" despite `whoami` working — plain retry succeeded), re-aliased `liquifi-app.vercel.app` (`Success!` 1s). Smoke: `/login` HTTP 200 in 0.93s; validation ladder proven live (`-F eventId=...` no image → 400 "Receipt image is required."). **User to re-test with a real receipt** (expect ~3-6s warm; watch for a single 8s abort + 1 reparse on the genuinely-cold first call). **Committed `f348a4a` (all post-step-23 work) → deployed `liquifi-9ssg5ao23` (build green 37s) → re-aliased `liquifi-app.vercel.app` (`Success!`, /login HTTP 200 in 1.08s).** ✅ **Verified live by user: real receipt uploads now parse in 3-5s (was 18s). Closed.**

---
- **2026-09-04 - Fixed untitled ₱0.00 "pending_ai_parse" orphan rows + cleaned the existing orphan in event "testing" (user report: "why is there an untitled entry in event testing").** Root cause: an AI-parse failure that ends in a _verdict_ (`invalid_document`/`borderline`/`multiple_documents` → 422) or a _duplicate_ (→ 409) left the **provisional Phase-1 row** (`status='pending_ai_parse'`, amount 0, all parse fields null) in the DB — an untitled entry that renders permanently. The `[entryId]` route deletes it on a 422 verdict but (a) didn't check the delete result, and (b) the **duplicate 409 branch never deleted it** — a live leak. **Fix** (`app/api/entries/receipt/[entryId]/route.ts`): added a shared `discardProvisional(insforge, entryId, imageUrl)` helper (DB delete with a thrown error so it can't silently orphan the row, + best-effort `deleteReceiptBlob`), and wired it into BOTH the verdict-422 path and the new duplicate-409 path. Note: the source was already tolerant on verdicts — the orphan in "testing" (`8f5186ed…`, an old 422 from a pre-fix deploy, logged `parsed in 2767ms → 422`) predates this route being committed (the two-phase receipt route is currently **untracked**, never deployed to the alias). **Cleanup:** deleted the orphan row (`DELETE FROM entries … status='pending_ai_parse'` — now `COUNT(pending_ai_parse)=0`) and its receipts-bucket blob key still exists in `storage.objects` (raw `storage.objects` delete is `permission denied`; a server-side `deleteReceiptBlob` run via the authenticated SSR client would clear it — not worth a custom function for a single dead blob, no UI references it). `tsc` + `next build` clean. **Not deployed** — the alias still serves the older build; deploy when the whole two-phase upload feature is committed + shipped together.

---
- **2026-09-04 - Log Entry: discard a parsed receipt now closes instantly and deletes in the background (user report: "discarding a parsed receipt loads first before the detail closes").** Previously `handleDiscard` awaited `discardReceiptEntry` and then cleared `parsedData` after a `setTimeout(…,150)`, so the review screen sat on "Discarding…" during the server roundtrip. Now both `handleDiscard` and `closeModal` (abandoned-review cleanup: X / overlay / drag-dismiss / Escape) clear `reviewOpen`+`entryId`+`parsedData` immediately to unmount the review UI, then fire `discardReceiptEntry(id, eventId)` fire-and-forget and `router.refresh()` when the row is gone. `entryId` is captured before nulling so the background delete still targets the right row; the `discarding` guard prevents double-delete on the X-vs-drag race. Deployed `liquifi-fiwd43dw4` → aliased to `liquifi-app.vercel.app`. `tsc` + `next build` clean.
- **2026-09-04 - Event page: entry timestamps + newest-first sorting (user request).** Entries now show their log timestamp (date + time) and sort newest→oldest by log time. (1) **Timestamp:** `EntryCard` (grid) gained a muted `date + time` line (`formatDateTime`); `EntryRow` (list) Date column previously showed date-only and now uses `toLocaleString` to include hour:minute. Applies to BOTH manual and receipt entries equally — the value comes from the DB `entries.created_at` column, set on every row insert regardless of type. (2) **Sort:** the "newest"/"oldest" sort in `ExpensesSection.tsx` previously used `id.localeCompare` (UUIDs don't order by creation time reliably); now sorts by `Date(createdAt)` time — newest = `ts(b) - ts(a)`, oldest = reverse — with an id tiebreak. Verified live on `liquifi-app.vercel.app` (deploy `liquifi-6yjwdpbpp`): event shows 4 entries as `12:52 PM → 12:03 AM → 12:02 AM → Aug 26 11:21 AM` (newest first), timestamps render in both list + grid views. `tsc` + `next build` clean.
- **2026-09-04 - Treasury bottom sheets: fixed "drag sheet down, reopen → only top part visible / never slides all the way up" (user report).** Root cause: the drag hook's `y` state lives in the **modal** (not the sheet subtree), and the modal persists across open/close — only the `{open && …}` subtree toggles. `useState(0)` therefore runs once for the modal's lifetime and is NOT reset when the sheet closes. A drag-dismiss left `y` dirty (~220px), and the next open rendered the inner sheet with `translateY(220)` inline — the sheet appeared partway up (top part only) and never slid to rest. Cold open was unaffected because `y` started at 0 before any drag. **Fix:** in `useDragToDismiss.endDrag`, when the drag crosses `DISMISS_DISTANCE` (>120px) and dismisses, reset the state: `setY(0)` + `startY.current = dragY.current = 0`, so the next open starts fresh at origin and the framer slide-up runs fully. Verified live on `liquifi-app.vercel.app` (deploy `liquifi-3pd98z1jq`) at 360×640: drag-dismiss closes, then reopen animates fully `top 440→196→172` and settles `bottom:640` (whole sheet visible, on-screen). Tap-outside close and cold-open slide-up still work. `tsc` + `next build` clean.
- **2026-09-04 - Treasury bottom sheets: fixed "only top part visible / animation looks unfinished" on real phones (user report).** Root cause: all mobile sheets capped height with `max-h-[85vh]`, but Chrome Android's `vh` is the LARGE viewport (includes the hidden URL bar / keyboard), so `85vh` overestimates the visible area → the sheet rendered taller than what's actually on screen and its bottom (Cancel) slid in behind the URL bar → looked like the sheet was cut off / the slide-up never finished. **Fix:** switched every `max-h-[85vh]` → `max-h-[85dvh]` (dynamic viewport height tracks the ACTUAL visible viewport, incl. URL-bar/keyboard collapse) across all 7 bottom sheets: `NewEventModal`, `LogEntryModal`, `EditBudgetModal`, `ArchiveEventModal`, `VoidEntryModal`, `ReceiptReview`, `EntryDetailModal`. Verified live on `liquifi-app.vercel.app` (deploy `liquifi-7pvxbaj1a`) at 360×640: entrance animates `top 426→199→172` and settles fully on-screen with the Cancel button visible at `y:586` (`cancelVisibleOnScreen: true`). `dvh` is universally supported (2023+); safe on desktop where `dvh`≡`vh`. `tsc` + `next build` clean.
- **2026-09-04 - Treasury bottom sheets: restored reliable slide-up entrance (user report: "add slide up animation when open").** v3's hook-owned entrance used framer's `animate(from, 0)` in a `useLayoutEffect` on the inner column — reproduced in production as a **frozen/stuck entrance** (sheet mounted partway, transform pinned at `translateY(180)`, never reached rest). **v4 (current):** entrance + exit moved onto the outer `motion.div` declaratively with the stock `sheetSlideUp` variant (`initial="hidden" animate="show" exit="exit"`) — same pattern the working non-hook sheets (`EditBudgetModal`, `ArchiveEventModal`) use. The hook (`useDragToDismiss`) now owns **drag + bounce + dismiss only**, `y` stays 0 until a real drag so nothing races the entrance. Removed the hook's `useLayoutEffect` entrance + unused import. Verified live on `liquifi-app.vercel.app` (fresh deploy `liquifi-53tppfnsy`): sheet rises `top 606→496→383→283→232` over 300ms (smooth slide-up, no freeze), and drag-from-anywhere still works (body pull → column `translateY(50)` then bounces back). `tsc` + `next build` clean; only remaining lint error is the pre-existing `LogEntryModal:61`.
- **2026-09-04 - Treasury bottom sheets: fixed "retracts under your finger" + "can't click after bounce" + "slide-up freezes mid-way + grip-only drag" (user report on the first fix).** v1 used framer's native `drag`/`dragElastic` per-sheet; reported broken: sheet retracts mid-gesture, taps swallowed on bounce. v2 (grip-strip + `useDragToDismiss` with pointer capture on pointerdown) fixed those but introduced TWO new complaints: (a) the framer `sheetSlideUp` entrance ran a transform on the outer `motion.div` while the hook's `y` state applied a second transform on the inner div — when the hook re-rendered (any pointer touch), framer's in-flight entrance froze mid-slide (only a click into it resumed it); (b) drag was limited to a thin 40px grip, not the whole sheet. **v3 (current):** `lib/use-drag-to-dismiss.ts` now owns the sheet's vertical position **entirely** — entrance slide-up (measured height, `useLayoutEffect`, no post-paint flash), drag, bounce-back, dismiss all on the single `y` state. The outer `motion.div` now only animates **exit** (`initial={false}`, inline `{y:'100%'}`), so no competing framer transform can freeze. Drag handlers + `touch-none` moved to the **whole sheet column** (grab anywhere). Pointer capture is deferred until travel > 8px, so a plain tap still lands on the button (clickability preserved after bounce). Wired into `NewEventModal` + `LogEntryModal`. `tsc --noEmit` clean, `next build` green; only remaining lint error in `LogEntryModal:61` is the pre-existing `set-state-in-effect` (confirmed not mine).
- **2026-09-04 - Treasury bottom sheets: fixed "retracts under your finger" + "can't click after bounce" (user request).** Root cause: `NewEventModal` and `LogEntryModal` used framer-motion's native `drag="y"` with `dragElastic={0.2}` + `dragSnapToOrigin`. (1) Elastic applies tension to the offset — pull past `top:0` and framer yanks the sheet *back up* toward origin even while the finger is still down (the visible "slides back while I'm holding" bug). (2) Framer's drag pointer-captures the whole sheet and its gesture system can swallow taps on release after an elastic re-position, leaving buttons unclickable. **Fix:** new `lib/use-drag-to-dismiss.ts` — a hand-rolled pointer-based drag (Pointer Events + `translateY` state, rAF-coalesced, `setPointerCapture`, and a one-shot `framer-motion animate()` for the bounce). The sheet follows the finger 1:1 (no elastic → never retracts mid-gesture); on **release** it dismisses if dragged ≥120px, else snaps cleanly back to origin. Because `touch-none` is applied only to a small **grip strip** (the top handle) — not the whole sheet — the form buttons live in the separate scrollable body and always receive normal taps, and the scrollable content still scrolls. Wired into both `NewEventModal.tsx` and `LogEntryModal.tsx` (grip strip replaced the old "grab-anywhere" `bodyRef` touch-swallow; `LogEntryModal` dismiss routes through `closeModal` so an abandoned `ai_parsed` row is still discarded server-side). Google Drive–style: follows finger while holding, dismiss-or-snap on release. Verified: `tsc --noEmit` clean on all 3 files, `next build` green. Pre-existing lint `react-hooks/set-state-in-effect` in `LogEntryModal` reset-on-open effect is NOT mine (confirmed on base commit). **Deployed:** `vercel --prod` + `vercel alias set` re-pointed `liquifi-app.vercel.app` (NOT `liquifi-blond`).
- **2026-09-04 - Fixed horizontal scroll from the mobile search input bar (user request).** Root cause: `flex-1` sets `flex: 1 1 0%` (flex-basis 0) but does NOT lower an input's `min-width: auto` shrink floor to the intrinsic content size (~20ch + padding). So on narrow screens the search input refused to shrink below its intrinsic width and could push past the viewport, producing horizontal scroll. Fix: added `min-w-0` to the search-mode input in `components/treasurer/MobileTopBar.tsx` so it can shrink to fit. Verified live on liquifi-app.vercel.app (deploy `liquifi-phhuh3a3d`): input has `min-w-0`, right edge 344 within a 360 viewport, `scrollWidth == clientWidth` (no overflow). (`flex-1` already allows grow; `min-w-0` is the shrink floor fix.)
- **2026-09-04 - Mobile search-mode fixes on `/treasurer/home` (user request).** (1) The `WELCOME BACK!` hero is now hidden while `?search=1` is active (`app/treasurer/home/client.tsx`), so search reads as a dedicated search page instead of the welcome screen. (2) Search results now actually render on mobile in search mode - previously only filter dropdowns plus a 'Search Results' label showed and matching events never appeared; now when `hasActiveFilters` it renders a `Search Results (N)` count plus the matched events as `ArchiveEventRow` rows (or an `EmptyState` when none match), matching desktop behavior. Confirmed live on liquifi-app.vercel.app: 'WELCOME BACK!' absent in search mode, `Search Results (7)` with rows for q=test, empty state for no matches. Horizontal-scroll note: search bar (`MobileTopBar`) and result rows already use `flex-1`/`min-w-0`/`flex-wrap`/`truncate` throughout - no fixed widths force overflow; could not reproduce overflow in emulator (emulation locks width), so no code change needed there.
- **2026-09-03 - Notifications page made immersive + red unread badge on top-bar bell + See-more pagination (user request).** `lib/event-route.ts`: `isImmersivePage` now also returns true for paths containing `/notifications`, so all 5 chrome consumers (MobileTopBar, Adviser/Admin top bars, MobileBottomNav, SidebarShell) hide chrome on the notifications page, which now owns its own back header. `components/treasurer/TreasurerLayoutShell.tsx` passes `unreadCount` into `MobileTopBar`; `MobileTopBar` renders a red `bg-error` badge (dot+count, `99+` cap) on the bell via `badge` when > 0. New `components/notifications/NotificationHeader.tsx` (client): back arrow (`ArrowLeft`) navigates to the role home (`/treasurer/home` or `/adviser/home`) + `Notifications` title + `N unread · tagline`. `components/notifications/NotificationsPage.tsx` (server): drops the old `max-w-3xl` wrapper for full-width immersive layout. `components/notifications/NotificationsList.tsx`: See-more pagination - shows 10 most-recent first, but `initialVisible = max(10, lastUnreadIdx + 1)` so if > 10 unread it surfaces all unread, then a 'See more' button (+10 each) reveals the rest. Verified: `tsc` clean, `next build` green, deployed `liquifi-flp79o9uw` aliased to liquifi-app.vercel.app (/login HTTP 200). Live (mobile 390): /treasurer/notifications shows immersive (top bar `display:none`), red badge `4` (rgb(239,68,68)=`--color-error`) on bell, 10 rows first + See more (10->20), back button -> /treasurer/home (top bar returns).
- **2026-09-03 - Treasurer home now shows 4 most-recent active events + always-visible Archive section w/ empty state & See-more pagination + new shared Active Events page (user request).** `app/treasurer/home/client.tsx`: Active Events header dropped the `ViewToggle` for a `View all ->` link (`next/link` to `/treasurer/events`); active grid now renders only `recentActive = allActive.slice(0,4)` (mobile folder grid / desktop EventCard grid). Archive section is ALWAYS rendered on both mobile + desktop (previously hidden when empty): 0 archived -> `EmptyState` ('No archived events yet'), else first 10 via `visibleArchived = archivedEvents.slice(0, Math.min(archiveShown, len))` + a `See more` button that bumps `archiveShown` by 10 (desktop keeps year-grouping filtered to the visible set). New shared page `app/treasurer/events/page.tsx` + `app/treasurer/events/client.tsx` (`/treasurer/events`, ALL active events, NOT limited to 4): header = back arrow (`router.push('/treasurer/home')`) + `Active Events` title + `Total of N Events` subtitle + `ViewToggle` far right; body grid (mobile `FolderCard` / desktop `EventCard`) or list (`EventListItem`) via `viewMode`. `lib/event-route.ts` `isImmersivePage` now also returns true for `/treasurer/events` so the global `MobileTopBar` hides and the page owns its top bar (static page wins over the `/events/[eventId]` dynamic segment - no conflict). **Deviation:** 'recent activity' = `created_at desc` (newest created) - the events table has NO `updated_at`/last-activity column (only `created_at`, `archived_at`), so 'most recently edited' cannot be honored without adding a column + touching every mutation; flagged for a follow-up if needed. Verified: `tsc --noEmit` clean, `next build` green, deployed `liquifi-aem3t55s2` aliased to `liquifi-app.vercel.app` (/login HTTP 200). Live (mobile 390x844): home shows exactly 4 folder cards + `View all` href /treasurer/events + 'No archived events yet' empty state; /treasurer/events shows 11 folder cards, back btn, grid+list toggle, list shows 11 EventListItem rows. Uncommitted (deploy-verified): `app/treasurer/events/page.tsx`, `app/treasurer/events/client.tsx`, `app/treasurer/home/client.tsx`, `lib/event-route.ts`.


## Current Status

**Phase:** Phase 11 — Audit & Read-Only Views (COMPLETE — all 30 build-plan features done) + post-plan hardening
**Last completed:** Bottom-sheet snap port (2026-09-07): all 8 `CssBottomSheet` sheets — 3 snaps 15/50/92%, velocity fling, dismiss-at-lowest-snap via new `onClose` (buy-in from admin `DepartmentsListClient` too). Root-caused + fixed the "every drag closes the sheet" regression of the 2026-09-07 native-drag deploy: the sheet-height measure effect ran with `sheetRef` null (deps `[]`, and `[open]` also ran too early) leaving `sheetHeight=0`, so all snap translates were 0 and the nearest-snap loop settled index 0 = dismiss; now deps `[mounted]`. All 3 CDP drag cases verified live (mid settle 170.5px / top settle flush / fast fling dismisses). `tsc` clean; commit + deploy pending. **Previous last completed:** Receipt parse — TRUE root cause found (see Decisions/entries, 2026-09-05): the model (`gemini-3.5-flash-lite`) never changed; the current route **uploaded the image then downloaded it back from storage** before calling Gemini, and those storage round-trips on a cold Hobby function blew the 8s budget → client retry doubled it to ~18s. `app/api/entries/receipt/route.ts` rewritten: the already-client-compressed multipart `File` feeds Gemini **directly** (no storage read), Gemini + `requireRole` run concurrently, blob upload overlaps the model call, and success inserts straight to `ai_parsed` (no provisional hop). Critical path ≈ 3-4s warm. Deployed `liquifi-mnca8h3h0` → aliased `liquifi-app.vercel.app`; `/login` 200 in 0.93s; route validation ladder proven live. ✅ **User-verified: real receipt uploads parse in 3-5s (was 18s) — closed.** (Prior entry same date: 2nd corrected fix after measured bench — model returns ~2-3s at any resolution, `MAX_ATTEMPTS` cut 3→2, `lib/image.ts` reverted to 1600/q0.8 — all still in place.) **Previous last completed:** Receipt-parse regression fixed for Vercel Hobby (see Decisions, 2026-09-04): `agent/receipt-parser.ts` no longer retries transport/timeout errors in-request — rows disappear immediately on click, the server action runs async in the background, and on failure the row is rolled back + the error surfaced (see Decisions, 2026-08-31). **Previous last completed:** Signup wizard back/forward state machine + 409 "just started" routing fix (see Decisions, 2026-08-31): step-aware back nav with prefill, reload-safe step index, per-email OTP cooldown that survives back-nav (resend countdown persists), and the step-3 re-proceed on an `in_progress` account now routes to `/otp` (original OTP still valid) instead of a blocking "just started" error — verified live on production (single `email_otps` row, no double-send). **Previous last completed:** Pending-aware overspend gate for manual entries (user direction: warn the treasurer before submitting if pending manual entries would push the event over budget; overspend fires ONCE per event). The manual-entry Step 18 gate in `submitManualEntry` (`actions/entries.ts`) now counts **committed = deducted + pending_approval** instead of deducted-only — the spent query went from `.eq("status","deducted")` to `.in("status",["deducted","pending_approval"])` — so a treasurer stacking pending entries gets the existing `overspendRequired` reason prompt before submission. New "fires once per event" guard: any entry already carrying `causes_overspend=true` (a durable flag — rejection doesn't clear it) → never re-prompt or re-flag. `lib/overspend.ts` unchanged (its helpers take the rows array as input); receipt `confirmReceiptEntry` gate untouched (receipts deduct immediately, no pending). Extended `scripts/check-overspend-gate.ts` with pending-inclusive + post-rejection-recross assertions. Verified: check script green, `tsc --noEmit` clean, `next build` green. Prior work in this line: client-side photo compression (commit `35c1f32`), manual-entry photo upload (commit `c857519`), budget immutability law (commit `87ef005`).
**Next (non-feature):** E2E smoke test of the read-only views + EventBrowser + profile pages (needs admin/adviser creds); verify/create the `push_subscriptions` RLS policy; deploy the notification-retention scheduled function (last two need MCP)
**Status:** All 30 features done (Phase 11 complete; checkboxes + total below reflect it). Post-phase: overspend gate is now pending-aware + fires once per event (manual entries count toward committed budget for the Step 18 prompt). Step 29 shipped the read-only layer: `readOnly` prop on `BudgetSummary`/`EventDashboardActions`/`SpendingBreakdownCard`/`ReportViewer` (mutating controls OMITTED, never disabled), `RejectedEntryActions` gated on `canMutate` (adviser leak fixed), proxy routes (image + pdf) now allow `"admin"`, three new pages — `/adviser/events/[eventId]`, `/admin/departments/[deptId]/events/[eventId]`, `/admin/departments/[deptId]/reports/[eventId]` — reusing the treasurer dashboard components, "View event" link in the adviser review header, admin dept Events/Reports tabs wired to real data (mock props deleted). Post-step refinements: `EventCard`/`EventListItem` gained an optional `href` prop (treasurer default `/treasurer/events/{id}` unchanged); new shared `components/events/EventBrowser.tsx` gives the adviser home + admin Events tab the full treasurer look — fake search bar → search mode (ArrowLeft exit, autofocus input) with Type/Treasurer/Date/Budget `FilterDropdown`s + "Search Results" divider, search-prompt + no-matches ("Clear filters") empty states, `ViewToggle` grid/list on Active Events (folder `EventCard` grid / `EventListItem` rows), Archive grouped by year + Sort dropdown — all staggered via `lib/motion-variants`. State-based search instead of the treasurer URL `?search=1` mode (that URL dance exists for the treasurer-only searchable MobileTopBar; adviser/admin layouts lack it). Verified after every step: `tsc --noEmit` clean + `next build` green. Working tree committed as `f348a4a` (2026-09-05) — only untracked junk remains (`.dev-err.log`, `.dev-out.log`, `figma/` download cache) — commit only when explicitly requested.

---

- **2026-09-02 - FolderCard now matches the Figma 'folder 1' design (user: 'the folder isn't accurate').** Old FolderCard drew a flat single-color silhouette (viewBox `404 263`, both paths `fill="currentColor"` = one `text-neutral` shade). Figma `folder` component set (`werwziocGxW0hh7a4sWvhl` node 57-32) is two-tone: light gray body `#D9D9D9` (front pocket + tab) outlined by a dark gray rim `#706D6D`. Fixed `components/events/FolderCard.tsx`: ported the Figma's exact path data (viewBox `353 246`, 3 paths) mapped to project tokens per the no-hardcoded-hex rule — rim `fill-neutral` (maps to `#71717a`, matches `#706D6D`), body+tab `fill-border-strong` (`#d4d4d4`, nearest to `#D9D9D9`). Kept hover animation + drop shadow. `tsc --noEmit` clean, `next build` green, compiled CSS confirms `.fill-neutral`/`.fill-border-strong` (verified old-vs-new: live production page had `404 263`/`currentColor` before). **Deployed:** `vercel --prod` -> `liquifi-f45a907u0` (Ready 56s, aliased `liquifi-blond`), then `vercel alias set` re-pointed `liquifi-app.vercel.app` (still manually pinned, does not auto-track). Live proof: `liquifi-app.vercel.app` now serves CSS `0xz8nbh` (was `3josiin4q30w0`) containing the folder fill utilities, and the deployed `/treasurer/home` DOM shows the new SVGs (`viewBox 353 246`, paths `fill-neutral` + `fill-border-strong` x2). EventCard NOT touched (different paper-sheet design, not the node pointed at).

- **2026-09-02 - Treasurer mobile home redesign (hamburger menu sidebar drawer) shipped + `liquifi-app.vercel.app` finally updated (root cause: stale manual alias, not a bad deploy).** Feature commit `355ae49` (working tree clean, `tsc` + `next build` green) -> fresh production deploy to the liquifi-app project (`liquifi-q34maxc87`, Ready 55s, aliased `liquifi-blond.vercel.app`). But `liquifi-app.vercel.app` still served the OLD build CSS `3fx1bkmkufai5.css` even after the clean redeploy - the default project domain was NOT following latest production. Root cause via `vercel alias ls`: `liquifi-app.vercel.app` had an explicit **manual alias** `liquifi-e4ru3il8n -> liquifi-app.vercel.app` pinned 19 days ago, overriding the latest-production auto-track. This also explains why every prior "re-aliased" entry here only ever affected `liquifi-blond.vercel.app`, never the domain the user cares about. **Fix:** `vercel alias set` re-pointed `liquifi-app.vercel.app` at the new deployment - now serves NEW CSS `3josiin4q30w0.css`, matching `liquifi-blond` (verified via live fetch; /login HTTP 200, no console errors). Note: / and /login are byte-identical old vs new (that feature only changed auth-gated /treasurer/home at phone width) - redesign provable only inside a logged-in treasurer mobile session; the CSS hash match is the deploy-level proof. **Deploy method:** `VERCEL_OIDC_TOKEN`. Future `--prod` alias auto-tracks `liquifi-blond`; `liquifi-app.vercel.app` tracks the SAME deployment only because we manually re-aliased it - if it reverts, re-run `vercel alias set <latest-url> liquifi-app.vercel.app`.

- **2026-08-31 — Signup wizard back/forward state machine + 409 "just started" UX gap fixed.** Added step-aware back navigation (function `backHref` support in `components/auth/AuthShell.tsx`; step-3 button "Go back" → step 2), field prefill when returning (email/role/dept restored from the persisted `signup_draft`), and a reload-safe step index persisted so a mid-wizard refresh lands back on the same step instead of step 1. Per-email OTP cooldown now survives back-nav: `/otp` derives its resend countdown from the persisted `signup_otp_sent` timestamp, so re-entering `/otp` (via back or re-proceed) shows the remaining real cooldown instead of resetting to 60s. Fixed the **409 "just started" dead-end**: re-submitting step-3 `complete` while the email's account is `in_progress` (a ghost that still exists after back-navigation) used to set a blocking `apiError` ("A signup with this email just started") with no way forward. Now `signup/page.tsx` step-3 handler, on `data.error.includes("just started")`, refreshes `sessionStorage.pending_signup` and `router.push("/otp?email=...&intent=signup")` (returns early: no blocking error, and it does **not** reset `otpSentAt` because no new send happened — `/otp` keeps showing the original cooldown). Rationale: a ghost account means the original OTP is still valid (ghosts purge after ~10 min, OTP valid 15 min), so routing to `/otp` is always correct. Only-`in_progress` accounts 409; genuinely-registered ones return the distinct "Email already exists" message. The 60s client cooldown is kept (avoids a round-trip + satisfies the requirement's letter). Requirement #4 (only-latest-OTP-valid) needed no code — InsForge keeps a single `email_otps` row per email+purpose, refreshed on resend. **Verified live on production** (`verify409` isolated context, email `verify409.fix@example.com`): step 1 no back ✓; step 1→2→3 via Continue ✓; step 3 → first submit created account + OTP and redirected to `/otp` (countdown 38s) ✓; back → step 3 restored (role/dept prefilled) ✓; re-proceed after cooldown → hit the 409 → **redirected to `/otp`** ("Verify your email") instead of a dead-end ✓; resend button re-enabled (cooldown elapsed, not reset). **DB proof of no-double-send:** exactly one `auth.email_otps` row for `verify409.fix@example.com` with `created_at = updated_at` (03:56:39Z) — the 409 re-proceed did NOT regenerate the code or refresh the row. `tsc --noEmit` clean, `next build` green, deployed to production, `liquifi-app.vercel.app` re-aliased. Test ghost purged (no leftover account).
- **2026-08-31 — Signup: OTP email now sends when the user reaches the /otp page, not when they first type their email on step 2 (user request: don't fire the verification email at form submit).** Previously step 2 `POST /api/auth/signup` called `insforge.auth.signUp(...)` immediately — creating the auth account and emailing the OTP code before the user had finished the 3-step wizard, so the code could arrive/expire before they ever saw `/otp`. There is no InsForge flag to "create the account but hold the email" (`autoConfirm` is admin-only and skips verification entirely), so the only real lever is **when the account is created**. Fix: `signup/route.ts` (step 2) is now a **validation-only gate** — it keeps the required-field check, the IP rate limiter, and the `check_signup_email` → 409 "Email already exists" duplicate check for early feedback, but no longer calls `signUp` (no account, no email). The account creation + OTP send moved into `signup/complete/route.ts` (step 3), the last wizard step that runs right before the `/otp` redirect. `complete` now: (1) requires `password` in the body and re-checks the same 8-char weak-password gate, (2) re-runs `check_signup_email` (registered → 409, guards the step-2→step-3 race) + purges any `in_progress` ghost, (3) calls `insforge.auth.signUp(...)` — this is the single point the account is born and the OTP email fires — then (4) resolves/auto-seeds the department. The IP rate limiter (`SIGNUP_IP_LIMIT` + `recordFailure`/`clearRateLimit`) moved there too so OTP-email bombing is still capped at the new send site. Client `signup/page.tsx` unchanged structurally: step 2 still calls `/api/auth/signup` (validation, weak-password 8-char client gate + duplicate feedback), step 3 now passes `password` to `complete` before `router.push('/otp')`. post-OTP `create-profile` (users row + notifications) is untouched. **Verified live on production:** after step 2 the email's `check_signup_email` state was `'none'` (no account, no email); after step 3 → `/otp` it was `'in_progress'` (account created + OTP sent at the boundary) and the OTP page loaded with the resend countdown. Test ghost purged. Entry still reflects the older signup/complete role in the 08-30 notification entry — that's historical. Verified: `tsc --noEmit` clean, `next build` green, deployed to production, `liquifi-app.vercel.app` re-aliased + HTTP 200. **Follow-up (same day): email-format flag moved to step 2.** The last-step "email is in the wrong format" surfaced because the malformed-email rejection came from `signUp` itself (now on step 3). Added a client-side format gate on the step-2 handler in `signup/page.tsx` — `EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/` (pragmatic, catches non-email before the last page; `ponytail:` comments mark it) — blocks `notanemail`, `user@nodot` with "Please enter a valid email address." right on the email page, and the user cannot proceed to step 3 until the email is well-formed (valid addresses still pass through; trimming applied on submit). This sits beside the existing 8-char weak-password gate. Verified live: `notanemail` + `user@nodot` blocked on step 2, `valid.email@example.com` advanced to step 3 with state still `'none'` (no account). `tsc` clean, `next build` green, deployed + `liquifi-app.vercel.app` re-aliased, HTTP 200.

- **2026-08-31 — Post-fix security hardening: removed the diagnostic leak + locked down two SECURITY DEFINER functions (from the adviser→admin notification fix).** The root-cause debug pipeline introduced a **live PII leak**: the `push_diag` table was created with RLS **off** and full `SELECT/INSERT/UPDATE/DELETE` granted to **`anon`** and `authenticated` — and the debug `diag()` calls had been writing real push subscription endpoints (`fcm.googleapis.com/fcm/send/...` tokens), user UUIDs, and applicant emails into it, all readable by the **unauthenticated** public role via the anon key. Also, two SECURITY DEFINER functions had `EXECUTE` granted to **`PUBLIC`** (reachable by `anon`): `get_user_push_subscriptions(target_user)` (an IDOR — returns ANY user's push endpoints/keys, no caller check) and the new `notify_signup_approvers` (lets any caller spoof a fake `adviser_signup_pending`/`treasurer_signup_pending` notification row into any active admin's/adviser's inbox). **Fixes: (1)** dropped `push_diag` and removed all `diag()` helper + call sites from `lib/web-push.ts`, `lib/notifications.ts`, `app/api/auth/create-profile/route.ts` (replaced throwaway diag calls with `console.error` so delivery failures stay debuggable without DB writes); removed the TEMP test-push artifacts (`app/api/notifications/test-push/route.ts`, `components/notifications/TestPushButton.tsx` + its import/mount in `NotificationsPage.tsx`). **(2)** `REVOKE EXECUTE ... FROM PUBLIC/anon` + `GRANT ... TO authenticated` on both `get_user_push_subscriptions(uuid)` and `notify_signup_approvers(text,uuid,text,text)` — so only authenticated sessions (which is exactly who the legit `create-profile` → `sendPushToUser` flow runs as, via `createInsforgeServer` reading the session cookie post-OTP) can call them. **Notification feature is unaffected:** the push path never breaks because the legitimate caller is `authenticated`, not `anon`. Verified: grants now `{authenticated, project_admin}` only, `to_regclass('public.push_diag')` = null, `tsc --noEmit` clean, deployed to production, `liquifi-app.vercel.app` re-aliased + `/login` HTTP 200. Broader hygiene flagged for later: sweep all remaining SECURITY DEFINER functions (e.g. `create_user_profile`, `purge_rejected_user`, `get_user_department_id`, `get_user_role`) for PUBLIC EXECUTE + unguarded arbitrary-parameter reads and scope each to its legit role.

---

- **2026-08-30 — Treasurer signup produced no `treasurer_signup_pending` notification — TRUE root cause found & fixed (this entry supersedes the earlier 08-30 diagnosis that blamed the users RLS policy): the notification fired BEFORE the `users` row existed.** The signup wizard is a 3-step flow: step 2 `POST /api/auth/signup` creates the **auth** account, step 3 `POST /api/auth/signup/complete` resolves/auto-seeds the department and (in the old code) persisted dept + fired the approver notification, then the flow redirects to `/otp`, and only **after OTP verification** does `POST /api/auth/create-profile` create the actual `users` ROW via `create_user_profile` (SECURITY DEFINER) with `department_id`. So `/signup/complete` ran when the signer's `users` row did **not yet exist**: its `.update({department_id})` no-op'd, and the adviser lookup's `get_user_department_id()` (reads `public.users` by `auth.uid()`) returned NULL → `.eq("department_id", NULL)` matched nothing → `if (adviser)` false → notification never fired, while the route still returned `{success:true}`. Empirically unforgeable: the CRIM treasurer `abbarrun30` (dept set to CRIM, `pending_approval`, created 10:33:55) — dept can ONLY be set by `create_profile`'s RPC — yet `notifications` had ZERO `treasurer_signup_pending` rows and `push_diag` (no RLS, so reliable) had ZERO ENTER rows at/after 10:33:55 → `createNotification` was never reached. This confirmed the old fix (persist dept inside the pre-OTP `signup/complete` route) could never work — the row doesn't exist yet. **Fix:** moved the approver notification into `app/api/auth/create-profile/route.ts`, running AFTER `create_user_profile` succeeds (so `department_id` + the row exist and `get_user_department_id()` resolves → adviser found). Both roles handled: treasurer → dept's active adviser; adviser → all active admins. Stripped the dead/misleading dept-persist + notify block out of `app/api/auth/signup/complete/route.ts` (now just validates role, auto-seeds the dept when missing, returns success). Verified: `tsc --noEmit` clean across the project.
- **2026-08-30 — Rejected signups now fully purge the applicant (user request: a rejected email stays "Email already exists" forever — "once the admin or adviser reject the account the information should be deleted from the database"):** rejection previously only set `account_status = 'rejected'`, leaving both the `public.users` profile and the `auth.users` auth record, so the signup classifier `check_signup_email` saw the email as `'registered'` (users row exists OR `email_verified`) and blocked re-registration permanently. Now both `rejectAdviserSignup` (`actions/approvals.ts`) and `rejectTreasurerSignup` call a new `SECURITY DEFINER` DB function **`purge_rejected_user(p_id uuid)`** instead of setting the status. The function deletes `notifications` + `push_subscriptions` first (NO ACTION FKs on `auth.users`), then deletes `auth.users` — which **cascades** to `public.users` + `auth.user_providers` — freeing the email for a fresh signup. FK survey confirmed a rejected applicant never has `entries`/`events`/`reports` (those `created_by` FKs are NO ACTION and would block, but a pending-rejected applicant creates none). `audit_logs` has **no FK** so the `user.rejected` audit row is preserved as an admin trail with the applicant's name/email captured before delete (deletion is the applicant's *account*, not the admin's audit record). The `signup_rejected` notification call was removed from both actions (the target user no longer exists; it would hit the notifications FK). Rejection email still sent (best-effort) from the captured email before purging. Function ACL: `{authenticated, project_admin}` only — no anon, no PUBLIC. Verified: function deployed via InsForge MCP with correct ACL, `tsc --noEmit` clean, deployed to production, `liquifi-app.vercel.app` + `liquifi-blond.vercel.app` re-aliased.
- **2026-08-30 — Pending-aware overspend gate + fires once per event (user request: "system should know only 400 left if this approve" — stack pending entries into the projected budget):** the manual-entry Step 18 overspend gate (`submitManualEntry` in `actions/entries.ts`) previously counted **deducted-only** as spent, so a treasurer could stack several `pending_approval` entries and only the much-later deduction would ever trip overspend. Now the gate counts the event's **committed total = deducted + pending_approval**, so on a ₱500 budget three ₱100 pendings (committed 300) show ₱200 left, and a fourth ₱300 submission (committed would be 600) triggers the existing `overspendRequired` reason prompt before the row is inserted — no deduction yet (adviser approval is still the gate, and rejection drops the row out of committed automatically because its status leaves `pending_approval`). The prompt fires **once per event**: a new `alreadyFlagged` query checks whether any entry for the event already carries `causes_overspend=true` (a durable flag — `rejectEntry` doesn't clear it), and if so the gate never re-prompts or re-flags even if the flagged pending is later rejected. Shared `lib/overspend.ts` unchanged (its helpers already take the rows array as input, so passing pending+deducted just works); the receipt `confirmReceiptEntry` gate is untouched (receipts deduct immediately — no pending). Approval re-derivation (`approvals.ts`) still re-derives the true deducted-only crossing and clears stale flags, so the pending-side flag is provisional until deduction. `scripts/check-overspend-gate.ts` extended with pending-inclusive money assertions + a post-rejection re-cross assertion. Verified: check script green, `tsc --noEmit` clean, `next build` green, deployed to production, `liquifi-app.vercel.app` re-aliased + `/login` HTTP 200.
- **2026-08-30 — Client-side compression for manual-entry supporting photos (user request: 8.5MB photo hung on mobile, 71KB was instant — "can the image compress upon uploading?"):** the manual-entry photo was being uploaded **raw** to `POST /api/entries/manual/photo` at full original size, so a multi-MB phone screenshot took far too long / appeared stuck on mobile data. The receipt path already had a proven client-side downscaler, so the fix was to **extract it** (not write a new one): `prepareImage()` moved out of `components/entries/ReceiptUpload.tsx` into a shared `lib/image.ts` (≤1600px longest side → JPEG q0.8 via canvas + `createImageBitmap` with `imageOrientation: "from-image"`, white-composited for PNG transparency, HEIC/HEIF + <1MB pass through untouched, any canvas failure returns the original file — downscale is an optimization, never a blocker). `ManualQuickForm.handleSubmit` now calls `await prepareImage(photoFile)` before the FormData append, so the 8.5MB file uploads as ~200–400KB. `ReceiptUpload.tsx` imports the same helper (private copy + constants deleted; net -50 lines, single source of truth). No server change — route's 10MB cap remains as a backstop. Verified: `tsc --noEmit` clean, `next build` green, deployed to production, `liquifi-app.vercel.app` + `/login` HTTP 200.
- **2026-08-29 — Mobile: hide Spending Breakdown on report page + immersive nav on report detail pages (user direction):** On `/treasurer/reports/[eventId]` the Spending Breakdown card is now `hidden lg:block` — gone on mobile (per-category breakdown stays on the event dashboard), kept on desktop. Separately, report detail pages now get the same immersive treatment as event pages: `lib/event-route.ts` adds `isReportDetailPage` (path contains `/reports/`, i.e. a trailing id, so the `/reports` list stays normal) and `isImmersivePage` (= `isEventPage || isReportDetailPage`); the 5 chrome consumers (`MobileTopBar`, `AdviserMobileTopBar`, `AdminMobileTopBar`, `MobileBottomNav`, `SidebarShell`) now use `isImmersivePage`, so the top bar + bottom nav slide away and content fills on `/treasurer/reports/[eventId]`, `/adviser/reports/[eventId]`, and `/admin/departments/[deptId]/reports/[eventId]`. Verified: `tsc --noEmit` clean, `next build` green, deployed to production, `liquifi-app.vercel.app` + `/login` HTTP 200.
- **2026-08-29 — Rejected reports stay visible on the event reports page (user request: don't remove a rejected report, list it like the archive pattern + newest at top with Cancel):** `/treasurer/reports/[eventId]` now renders the **full report history** instead of only the latest. New `getAllReportsByEvent(eventId)` (`lib/queries/reports.ts`) returns every Report row for the event ordered by `generated_at` desc (reports are never overwritten — regeneration = new row). The page derives `latestReport = reports[0]` + `olderReports = reports.slice(1)`; the newest report keeps driving the existing precondition gate (pending/approved lock, generation flow, `ReportViewer` with Cancel-if-pending) exactly as before, and a new **"Previous revisions"** card below lists every superseded report (rejected/cancelled/approved) as a `ReportFileCard` row labeled by its status badge — so after an adviser rejects, the rejected PDF stays reachable (View/Download) below the current report. Scoped to the treasurer page only (adviser/admin read-only mirrors untouched). Verified: `tsc --noEmit` clean, `next build` green (all 38 routes), deployed to production, `liquifi-app.vercel.app` + `/login` HTTP 200.
- **2026-08-29 — Report pages → premium file-card pattern + PDF download (user direction: no inline full-PDF render, report = a downloadable document, + download capability):** the report's PDF is no longer rendered inline anywhere — `PdfViewer.tsx` deleted (`pdfjs-dist` now unused but left in `package.json` to avoid churn/risk). New shared **`components/reports/ReportFileCard.tsx`** (server component, zero client state) renders the report as a compact "file" card on every surface: rounded-2xl `border-border-strong bg-surface shadow-card`, `FileText` tile on `bg-accent-light` (h-12 w-12 rounded-xl), FS number + status chip + "Liquidation financial statement report" subtitle, and two actions — **View** (filled-accent `rounded-full`, opens `/api/reports/{id}/pdf` inline in a new tab) + **Download** (outline border button, `<a href="?dl=1" download>` → attachment). Buttons `flex-1` on mobile, `flex-none` from `sm:`. Only `@theme` tokens (`text-accent`, `bg-accent-light`, `border-border-strong`, `text-text-muted`, `shadow-card`...). Wired into `ReportViewer` (treasurer detail/read-only), `ReportGenerationFlow` preview (header stripped as redundant), + `AdviserReportReview` "Signed report" section (client file card no longer double-renders a mock preview). **PDF download added via the existing proxy route** `app/api/reports/[reportId]/pdf/route.ts`: default stays `inline` (View), `?dl=1` switches `Content-Disposition` to `attachment`. Also shipped two held changes: `proxy.ts` rewritten so `updateSession` runs FIRST and redirect decisions use the real post-refresh `accessToken` (closing/reopening browser no longer bounces an actually-logged-in user to `/login` — the 2-day refresh cookie now slides instead of the pre-refresh no-token cookie winning), and `EventDashboardActions.tsx` mobile button scale (Log Entry `text-[14px]` + `h-4 w-4` Plus). Verified: `tsc --noEmit` clean, `next build` green (all 38 routes). UI verified via a temporary `/dev/report-preview` route (snapshot + getComputedStyle: card radius 16px, border `rgb(212,212,212)`, layered shadow, tile `rgb(238,238,239)`, V/D `rounded-full` filled/outline, 390px → side-by-side buttons) then deleted. **Not blocked, but:** production domain (`liquifi-app.vercel.app`) still needs adding in the Vercel dashboard (CLI can't do it); no seeded creds → authenticated report pages not browser-verified.

---

## Progress

### Phase 0 — Authorization Foundation

- [x] 00 Route × Role × Precondition Matrix

### Phase 1 — Foundation

- [x] 01 Landing + Auth Shell
- [x] 02 Database Schema
- [x] 03 Auth — Signup, OTP, Approval Routing
- [x] 04 Login, Session, Role-Based Redirect

### Phase 2 — Admin: Departments & Approvals

- [x] 05 Admin Departments Page — Full UI
- [x] 06 Admin Departments — Real Data + Mutations
- [x] 07 Admin Approvals — Adviser Signups

### Phase 3 — Adviser: Approvals

- [x] 08 Adviser Approvals Page — Full UI
- [x] 09 Adviser Approvals — Real Data + Mutations

### Phase 4 — Treasurer: Events & Budget

- [x] 10 Treasurer Home + Event Creation — Full UI
- [x] 11 Event Creation + Budget Lock — Real Data
- [x] 12 Event Dashboard — Full UI

### Phase 5 — Treasurer: Logging Entries

- [x] 13 Entry Logging — Full UI
- [x] 14 Receipt Parsing — OpenRouter Integration
- [x] 15 Receipt Confirm / Discard + Budget Deduction
- [x] 16 Manual (No-Receipt) Entry Submission

### Phase 6 — Voiding & Overspend

- [x] 17 Void Entry — Full UI + Logic
- [x] 18 Overspend Explanation Flow

### Phase 7 — Reports: Generation

- [x] 19 Report Generation — Full UI
- [x] 20 Report Generation — Real Logic
- [x] 21 Cancel Pending Report

### Phase 8 — Reports: Adviser Review

- [x] 22 Adviser Report Review — Full UI
- [x] 23 Report Approve / Reject — Real Logic

### Phase 9 — Archiving

- [x] 24 Archive Event — Full UI
- [x] 25 Archive Event — Real Logic

### Phase 10 — Notifications

- [x] 26 Push Notifications — Full Setup
- [x] 27 Notification Retention Job

### Phase 11 — Audit & Read-Only Views

- [x] 28 Audit Log — Admin View
- [x] 29 Adviser & Admin Read-Only Event/Report Views

**Total: 30 / 30 features complete**

---

## Decisions Made During Build

*Condensed 2026-08-08 (twice) on request — full verbatim history preserved in git.*

- **2026-09-04 (2nd, corrected after measured bench) — "still 20-30s" + "can't scan" after the 1st fix → the downscale-to-1280 hypothesis was WRONG; measured and reverted.** Pulled prod logs: every `/api/entries/receipt` AND `/[entryId]` call throws `TimeoutError: aborted due to timeout` (code 23) — my Phase-1 kill of the server retry loop was in place, so the 20-30s was the **client** multiplier: `ReceiptUpload.handleUpload` ran `MAX_ATTEMPTS=3` → primary (8s timeout) + 2 reparse retries, each an 8s-cold round trip + 900ms backoff ≈ 25s. **Measured Gemini directly (bench script, real key, real phone-sized 3024x4032 receipt):** the model returns in **~2-3s at ANY resolution** — prompt tokens pinned ~1065 regardless of 1024/1280/1600/raw (Gemini resamples internally), so **downscaling gives ZERO speed benefit** and at 1024 it GARBLES the text (SVG source leaked, 94-token output) — confirming the user's "you compressed it too much, AI can't read it." **Therefore:** (1) **reverted `lib/image.ts`** `MAX_DIM 1280→1600`, JPEG q `0.7→0.8` — restores legibility, no speed cost (bench-proven); (2) **cut client retry multiplier** `MAX_ATTEMPTS 3→2` (primary + 1 reparse) in `ReceiptUpload.tsx`, halving worst case ~25s→~17s; (3) `lib/gemini.ts` `TIMEOUT_MS` stays `8_000` (Hobby 10s ceiling — cannot safely exceed; a 9s timeout risks the function being killed at 10s, and cold-start overhead eats the headroom regardless). **Honest residual wall:** on Vercel Hobby's 10s ceiling, a genuinely COLD function (low-traffic Hobby sleeps aggressively) whose start + upload + first inference exceeds ~8s still aborts; warm calls are ~2-3s. Revolved/compression both fixed; the cold-start risk is inherent to Hobby inline and cannot be eliminated without upgrading or accepting background. Deployed to `liquifi-app.vercel.app`, `/login` HTTP 200. **User should re-test a real receipt: expect ~3-6s warm, readable parse restored.**

- **2026-09-04 — Receipt parse regression root-caused + fixed (user: "check why the AI keeps 10-15s before succeeding; before it only took 5-9s end to end"; user also confirmed target plan is Hobby plan, NOT Pro):** the inline receipt parse inflated from ~5-9s to ~20s. **Proven root cause (git diff):** an uncommitted `TIMEOUT_MS` change from `120_000` → `8_000` (`lib/gemini.ts`); the committed baseline was `120_000`, where a 5-9s Gemini call returned on attempt 1 and `parseReceipt`'s internal 3x retry never fired. At `8_000`, a receipt needing ~9s aborted at 8s and the internal retry multiplied it to 24s per route, then the client's `[entryId]` reparse added more. **Plan change forced by user:** they will use **Vercel Hobby**, not Pro — so the 10s function ceiling means a single inline Gemini budget is ~8s and raising the timeout is useless (the platform kills the request at 10s). Fix is therefore NOT a timeout bump but: **(1) kill the retry multiplier** — `agent/receipt-parser.ts` now treats transport/timeout errors (`GeminiError`, `TimeoutError`, `AbortError`, `/aborted|timeout|timed\s?out/i`) as **non-retryable** (throw immediately) so a timed-out Gemini call isn't re-issued 3x in one shared-10s-budget request; the **3-attempt budget is preserved only for fast zod/schema-mismatch re-parses** (the model already returned; re-running is cheap) per `library-docs.md`. New helper `isTimeoutError(err)`. The one-shot `[entryId]` route (fresh ~8s budget, same blob) remains the correct retry lane for hard receipts, per user choice — no polling loop, no background. **(2) image downscaling is the real 2-4s lever on Hobby** — `lib/image.ts` `MAX_DIM` 1600→1280, JPEG q 0.8→0.7 (fewer input tokens → earlier Gemini first token; this was the "10-15s" driver, not the model). A/B **measure** with the `parsed in ${ms}ms` Vercel log, keep the setting that's <8s reliably + ~2-4s typical without new borderline verdicts; if tiny-print thermal receipts regress, bump back toward 1600. **`TIMEOUT_MS` stays `8_000`** (correct for Hobby). Honest Hobby ceiling: a genuinely hard receipt (model needs >8s) tops out at two ~8s request windows (~16s) via the one-shot `[entryId]` retry — that's better than the old 20s and actually succeeds; typical receipts hit 2-4s. Verified: `tsc --noEmit` clean, `next build` green, deployed to `liquifi-app.vercel.app` (alias re-pointed), `/login` → HTTP 200 + full Liquifi HTML.

- **2026-08-31 — Optimistic UI on user approve/reject (user direction: "instead of waiting for the server round-trip to finish before showing success, update the UI immediately and let the actual mutation happen async in the background"):** the admin/adviser approval flows previously awaited the server action, then edited their local row list only on `success` — so the UI froze on a spinner until the `users` update (and email/notification best-effort work) finished. Now both flows remove the row **immediately** on click and run the server action async in the background. **`components/admin/AdminApprovalsClient.tsx`** (`handleApprove`/`handleReject`): capture `removed = localApplicants.find((a) => a.id === userId)`, `setLocalApplicants(prev => prev.filter(...))` right away, `await approveAdviserSignup/rejectAdviserSignup`; on `success` → `router.refresh()`; on failure → **roll back** (`setLocalApplicants` re-prepends `removed` only if it isn't already present) + surface `actionError`. Dependencies updated (`[router, localApplicants]`) so the captured `removed` closure stays in sync. **`components/adviser/AdviserApprovalsClient.tsx`** (`executeUserAction`, used by both `approveTreasurerSignup`/`rejectTreasurerSignup`): same pattern — capture `removed`, filter immediately, `setConfirmingAction(null)` (dialog closes instantly), run the action; on failure roll back + `setError`; on success `router.refresh()`. Server `actions/approvals.ts` unchanged — it remains the source of authority (still `requireRole`-gated, still validates pending+role, still runs purge/email/notifications); optimistic UI is purely a client rendering decision, and rollback only happens when the server rejects. Verified: `tsc --noEmit` clean, `next build` green.
- **2026-08-31 — Follow-up: instant reject + double-click guard (user: "theres still delay and dead air when i click reject... make it respond instant and just approve or delete it in the background. also for adviser theres no spinner and the button can be click again... disable the button or have a spinner to prevent double clicking", then "the instant with no dead air should also apply for adviser"):** three refinements on top of the optimistic UI. **(1) Admin** — the `window.confirm` modal was the "dead air": it blocks the UI thread *and* rejection still ran as a blocking awaited call. Removed the native confirm entirely and unified approve/reject through one `runOptimistic` helper: both now remove the row instantly on click, run the (async) background server action, roll back on failure, and add a `pendingRef: Set<string>` guard so a rapid double-click can't fire the same background action twice before React re-renders the row away. **Note:** historically reject confirmed before an irreversible purge; per explicit user direction the confirm is now gone, so an accidental Reject click *successfully* purges the user (rollback only guards server-side preconditions). **ponytail:** this trades a safety confirm for the mandate of instant/background — if a confirm is ever wanted again it must be the app's own non-blocking styled dialog, never `window.confirm`. **(2) Adviser (first pass)** — `executeUserAction` previously closed the confirm dialog instantly and never set the shared `approving` state, so the dialog button stayed enabled and a double-click fired `approveTreasurerSignup`/`rejectTreasurerSignup` twice. First pass kept the dialog open with a `Loader2` spinner + disabled button (Cancel too) for the whole action — but that was still dead air. **(3) Adviser (final, per second direction)** — `executeUserAction` is now **instant** like admin: on dialog confirm it removes the row + closes the dialog NOW and runs the server mutation (incl. reject purge) async in the background, rolling back + surfacing `setError` on failure and `router.refresh()` on success. Double-click protection is a `userActionRef: Promise<void>` gate (single in-flight action per user) instead of a spinner, since the dialog closes instantly. The batch-approve path keeps its `approving` spinner/disabled (bulk action, still awaited). Verified: `tsc --noEmit` clean, `next build` green, deployed live (liquifi-app.vercel.app → HTTP 200).
- **2026-08-31 - Follow-up: audit logs made human-readable + filterable (latest compliance/accountability direction: "guarantee audit logs for compliance/accountability ... logged permanently so the org can reconstruct exactly who did what and when. also add filter so the admin can filter all the audit logs from who did that and when"):** the Audit Logs tab previously rendered raw `action` codes (`entry.confirmed`), the raw `target_type`, and a `JSON.stringify` dump of metadata ("a bunch of code") with zero filtering. Rebuilt it as a readable, compliance-oriented trail. **(1) New `lib/audit-log-view.ts`** - a pure mapper turning every `action` + `metadata_json` into `{ label, category, icon, tone, summary, details }`: human labels ("Receipt parsed", "Report approved", "User rejected - account purged"), one-line summaries reconstructed from metadata (amount in PHP, document #, FS number, from/to budget, rejection reason, revision/signatory counts, blockchain anchor), and structured `details` rows for the expanded view - **no raw JSON anywhere**. All 23 mapped actions incl. receipt verdicts, entry lifecycle, report lifecycle, user approve/reject/deactivate, event create/budget/archive, department create/activate; unknown/legacy codes fall back gracefully. **(2) `app/admin/departments/[departmentId]/page.tsx`** - rows now carry `actor_id` + `actor_role` (role resolved alongside name); exposes a distinct `auditActors` list for the "who" filter. **(3) `DepartmentDetailClient.tsx` `AuditTab`** - added a filter bar: **Who (actor)** dropdown, **Category** dropdown (derived from the mapper), **From date** / **To date** native date inputs, and a Clear button. Client-side filtering over the server-fetched list. Rows show a tinted icon square (per-category tone), human label + summary, actor name + role, readable date-time; expandable rows show structured detail rows (desktop table + mobile cards), "Showing X of Y" count when filtered, and an EmptyState when filters match nothing. Verified: `tsc --noEmit` clean, `next build` green. **ponytail:** filters are client-side over the `.limit(100)` fetch - if a department ever exceeds 100 logs and the admin needs to reach further back, that's the cue to add server-side pagination; per-log debit is fine at this scale.

- **2026-08-30 — Manual-entry supporting photo fixed on mobile (bug: library pick always failed with "Failed to submit. Please try again."):** root cause — the manual form was passing a `File` to a Next.js Server Action in a plain-object payload (`submitManualEntry`'s `photoFile`), which this built can't serialize; the client-side `catch` in `handleSubmit` swallowed the invoke throw into the generic failure string. Camera captures hit the same path bought failed, but the user only noticed the library pick. Fix mirrors the existing receipt transport (which already routes files via `FormData`/`fetch`, not server actions): **(1)** new `app/api/entries/manual/photo/route.ts` — `POST` FormData (`eventId`, `entryId`, `image`) guarded by `requireRole("treasurer")` + own-dept + event `open` + not report-locked (runs BEFORE upload), validates image mime/size, calls `uploadReceipt`, returns `{ success, key }`; no Entry row created here — the row is still only inserted later by `submitManualEntry`. **(2)** `ManualSubmitPayload` (`components/entries/ManualQuickForm.tsx`): `photoFile: File | null` → serializable `entryId: string` + `imageKey?: string`. **(3)** `handleSubmit` uploads the photo client-side via `fetch`/`FormData` before invoking the action (key cached on `uploadedImageKeyRef` so a gate-triggered resubmit reuses it instead of re-uploading — storage is `upsert:false`); on upload failure it shows a form error and aborts with NO write. **(4)** `submitManualEntry` (`actions/entries.ts`): drops the server-side `uploadReceipt(file)` step, validates `payload.entryId` (UUID) + uses `payload.imageKey` as `image_url` for the insert and audit's `has_photo`. Invariant preserved: a failed upload ⇒ no row; a successful upload with a later gated/insert-failed submission leaves only a dead-blob orphan (existing precedent). Verified: `tsc --noEmit` clean, `next build` green (38 routes incl. new photo route).

- **2026-08-30 — Budget editable only while untouched, immutable once any entry exists (user decision: build the missing edit UI + "any entry incl. pending locks", no separate reduction trigger):** the app had NO budget-edit capability — `budget_total` was fixed at event creation (`createEvent`). Built it from scratch with the new immutability law as the guard. **(1) `lib/budget-lock.ts`** — `deriveBudgetLocked(statuses)` simplified from "any `deducted`/`voided`" to `statuses.length > 0` (any entry row, any status, locks the budget; a void never reopens it). Both call sites in `lib/queries/events.ts` inherit automatically (`getDepartmentEvents`' `everDeductedIds` renamed `anyEntryIds`; `getEventDashboard` already passed the status list). **(2) `actions/events.ts`** — new `updateEventBudget(eventId, newBudgetTotal)` server action: `requireRole("treasurer")` + department-scoped fetch, rejects `archived` / any `pending_adviser_approval|approved` report (`is_locked`) / any existing entry row (`budget_locked`, checked via `limit(1).maybeSingle()`), validates positive amount, updates `budget_total`, writes an `audit_logs` row (`event.budget_updated` with from/to), `revalidatePath`s home + event page. **(3) UI** — new `components/events/EditBudgetModal.tsx` (reuses `EventForm` number-input + `NewEventModal` dialog/sheet + `motion-variants` patterns, calls `updateEventBudget`); `BudgetSummary` gained a `budgetLocked` prop + `Pencil` "Edit Budget" button in the desktop action column and a compact "Edit" pill on the mobile card, both `disabled` with a plain-language tooltip (archived → report pending → any-entry rule) and **omitted entirely under `readOnly`** (adviser/admin). `app/treasurer/events/[eventId]/page.tsx` passes `budgetLocked={event.budget_locked}` to both mobile + desktop renderings. Net effect: the dark card and its drill-down show the correct locked state, and a treasurer can raise/lower the budget only before the first expense (even a pending manual entry locks it). Verified: `tsc --noEmit` clean + `next build` green, pushed `87ef005`, deployed, re-aliased `liquifi-app.vercel.app`, `/` + `/login` HTTP 200.

- **2026-08-30 — Report list pages regrouped by report life-cycle state (user reframe: the report page is the system of record / audit-trail + approval interface for the liquidation workflow, so organizing by event presence was wrong):** replaced the "With reports / No report yet" presence split on both `app/treasurer/reports/page.tsx` and `app/adviser/reports/page.tsx` with report-lifecycle groups. **Treasurer:** `Needs attention` (no report OR `rejected`; `cancelled` collapses here via `getLatestReportsByEvent`, which drops a newest-cancelled event back to `null` — correct, since cancel → needs regeneration; the cancelled PDF stays reachable on the detail page's Previous revisions) → `Pending approval` (`pending_adviser_approval`) → `On file` (`approved`, event still `open`) → `Archived` (`event.status === "archived"`, always carries an approved report; read-only rows). **Adviser:** keeps excluding no-report events (nothing to review) but regroups into `Pending approval` (awaiting your review) → `Reviewed` (`approved`/`rejected` decision trail) → `Archived`. Every section renders only when non-empty; global `EmptyState` fires only when the full row set is empty; archived extracted from `active` so it never leaks into the workflow groups. Row markup reused verbatim (solid + `StatusBadge` for report-bearing, dashed + "No report yet" pill for null; badge kept on every row per user decision). No data/guard/schema changes — both pages already source `event.status` (`getDepartmentEvents`) and newest report (`getLatestReportsByEvent`). Verified: `tsc --noEmit` clean, `next build` green.

- **2026-08-25 — Multiple-receipt photos get their own verdict (user report: two receipts in one image said "too blurry" instead of the one-per-upload rule):** the classification enum had no outcome for multi-document images, so Gemini's doubt-rule pushed them to `borderline` ("photo looks blurry") — the one-receipt rule was never communicated at failure time. Added a 4th outcome: `classificationSchema` enum + `ParseOutcome` union gain `"multiple"` (`agent/types.ts`); prompt rule 8 teaches it (2+ distinct documents, even partially visible → `multiple`, never extract; hand/fingers ≠ second document; doubt multiple-vs-borderline resolves by document count) (`agent/receipt-parser.ts`); route maps it to 422 `multiple_documents` with guidance "This photo contains more than one receipt. Upload one receipt at a time…" + audit `entry.receipt_multiple_documents` — verdict-class, never counted toward the 3-attempt ceiling (`app/api/entries/receipt`); `ReceiptUpload` renders a "One receipt per upload" warning banner with a single "Try Another Photo" action (no No-Receipt CTA) + a proactive mobile hint under the camera/library chooser ("One receipt per photo — shoot each receipt separately." — desktop dropzone already had it). Docs: library-docs classification section + registry ReceiptUpload entry updated; AGENTS.md outcome list left for `/sync`.

- **2026-08-25 — Event page immersive navigation (user report: nav bars clutter the event detail view):** on all 3 event detail pages (`/treasurer/events/[eventId]`, `/adviser/events/[eventId]`, `/admin/departments/[deptId]/events/[eventId]`), mobile top bar slides up (`-translate-y-full opacity-0`) and bottom nav slides down (`translate-y-full opacity-0`) over 300ms via CSS transitions — shared `isEventPage(pathname)` helper in `lib/event-route.ts` checks `pathname.includes("/events/")`. `SidebarShell` animates `pb-20` → `pb-0` on event pages (existing `transition-[padding] duration-300`). Adviser + admin inline top bars extracted into client components (`AdviserMobileTopBar.tsx`, `AdminMobileTopBar.tsx`) for `usePathname()` access. Event page content wrapped in `EventPageEntrance` (framer-motion: fade + slide-up 12px over 400ms). Back navigation via existing ArrowLeft in page headers. Desktop layout unaffected (sidebar already `lg:flex` only).

- **2026-08-25 — Event dashboard mobile hierarchy (user direction: too crowded, sections need their own space, Archive beside title):** all three event-page shells (treasurer + adviser/admin read-only mirrors) replaced uniform `gap-5` with deliberate spacing — header `mt-3`, locked/archived banner `mt-5` (conditional wrapper so spacing is stable when absent), hero card `mt-5`, mobile action strip `mt-3` (grouped with card), Expenses isolated in `mt-8 border-t border-border-light pt-6` section break. Header no longer wraps: `flex items-center justify-between gap-3`, title `min-w-0 truncate text-lg sm:text-2xl md:text-[28px]` (long names ellipsize on phones), **ArchiveEventButton sits far right beside the title** (was wrapping under the header) and is compact on mobile (`px-2.5 py-1.5 text-xs rounded-lg` → `sm:px-4 sm:py-2.5 sm:text-sm sm:rounded-xl`, `shrink-0`). `BudgetSummary` scaled down for phones (`p-4 sm:p-6`, remaining `text-[26px] leading-8 sm:text-[40px] sm:leading-12`, gaps `gap-4 sm:gap-6` / `mt-3 gap-6 sm:mt-4 sm:gap-8` / progress `mt-4 sm:mt-6`) — desktop untouched. Shared components, so all three roles inherit; `readOnly` semantics untouched.

- **2026-08-25 — Item breakdown Qty/Price/Amount blank for receipts (user report):** the DB holds two shapes — receipts store the Gemini zod schema verbatim (`{description, qty, unitPrice, lineAmount}` camelCase, `app/api/entries/receipt` inserts `parsed.item_breakdown` as-is) while manual entries store snake_case (`{description, qty, unit_price, line_amount}`, `actions/entries.ts`). `EntryDetailModal` read a third shape (`quantity`/`unit_price`/`line_amount`) → receipts rendered "—" in all three numeric columns (manual Qty too). Fix: tolerant `normalizeItem` in `EntryDetailModal` (accepts `qty ?? quantity`, `unitPrice ?? unit_price`, `lineAmount ?? line_amount`, numeric strings coerced); both shapes render fully now. `ReceiptReview`/`LogEntryModal` untouched (they render the camelCase `ParsedReceipt` at review time — already correct); adviser report detail inherits via `EntryDetailModal`.

- **2026-08-25 — Persistent sliding session (user request: active users must stay logged in across browser opens):** root cause — the `insforge_refresh_token` cookie lifetime was inconsistent per surface: login (`createAuthActions`) and every proxy refresh (`updateSession`) used the SDK default (`expires = refresh-JWT exp`, or **no Expires at all → browser-session cookie**), so login users were logged out every time the browser closed; only the OTP-verify manual path persisted (30d, inconsistent). Fix via the SDK's cookie-option override (`options: { refreshToken: { maxAge } }` — Max-Age beats Expires per RFC 6265; supported at all three entry points, verified in SDK source): new `lib/session.ts` exports `REFRESH_COOKIE_MAX_AGE` (2 days) + `REFRESH_COOKIE_OPTIONS`; passed into `updateSession` (`proxy.ts` — re-pins the cookie on every navigation, making the window **slide**), `createAuthActions` (`app/api/auth/login`), `createRefreshAuthRouter` (`app/api/auth/refresh`); OTP verify manual cookie aligned to the shared constant (was 30d). `user_role` cookie bumped 2d → 7d (cosmetic landing-redirect hint must outlive the sliding window). Security posture unchanged (httpOnly + secure + lax; logout's `expiredCookieOptions` strips overrides and clears regardless). Away >2 days → re-login (by design, adjustable via the one constant).

- **2026-08-25 — EventCard mobile overflow fix (user direction):** at 2-col phone widths (~169px cards) the paper's fixed `h-[82%]` content box overflowed — long `formatPHP` amounts wrapped the budget row, and flexbox collapsed the `line-clamp-1` name to invisible (overflow-hidden items have flex min-size 0). Fix in `components/events/EventCard.tsx` (shared by treasurer home + EventBrowser, so all three roles inherit): name `line-clamp-2 shrink-0 leading-4 sm:line-clamp-1 sm:leading-snug`; budget text `truncate min-w-0` + `title` full string (formatPHP precision untouched — ui-rules currency rule honored); pct `shrink-0 pl-2` colored by budget range on mobile only (`sm:text-text-muted`); **progress bar `hidden sm:block` — removed from mobile cards (user direction)**, pct color now carries the status signal there; creator `hidden min-w-0 truncate sm:block` + entries span `ml-auto` (stays right-aligned when creator hidden); paper `gap-1 sm:gap-1.5`. Worst-case mobile content ≈74px ≤ 81px box; desktop rendering unchanged. Also this session: X-style nav restyle (filled-icon active states via a `FILLED` map in `components/layout/NavItem.tsx`, active pill removed, bottom-nav icons 24px) + sidebar tokenized via new `nav-*` @theme tokens (`--color-nav/-hover/-active/-border`) — zero hex left in `components/layout/`; documented in ui-tokens/ui-rules/ui-registry.

- **2026-08-23 — Pending page logo-to-card gap tightened (user direction):** wrapper `pt-16` → `pt-6`; logo→"Awaiting Approval" gap 96px → 56px (logo's own mt-8 headroom preserved). Verified 390×844: gap 56px measured, zero scrolling.
- **2026-08-23 — Logo top margin on backless auth pages (user direction):** `AuthShell` logo link gains `mt-8` only when `top && !backHref` — i.e., exactly the pending page, whose logo previously touched the screen edge. Signup/OTP keep their back-arrow-first look; login (centered, hideLogo) unaffected. Verified 390×844: logo at y=32, scrollHeight == viewport — zero scrolling.
- **2026-08-23 — Pending page top margin (user direction):** no back arrow on this page (unlike OTP/signup) made the top-aligned card feel cramped — wrapper `pt-4` → `pt-16` (64px breathing room). Verified 390×844: card title at y≈124 (was ~90), scrollHeight still == viewport — zero scrolling.
- **2026-08-23 — OTP + pending pages top-aligned for mobile (user direction):** both auth pages now match the signup pattern — `AuthShell top` + inner `pt-4` wrapper. OTP page dropped its redundant shell-level subtitle (card subtitle already carries "code sent to {email}" info); pending page's triple `mt-24` stack (wrapper + Lottie + button, 96px each) shrunk to `pt-4` + `mt-8` + `mt-8`. Verified on 390×844 viewport: both pages scrollHeight == viewportHeight — zero scrolling needed.
- **2026-08-23 — Signup hygiene DEPLOYED via InsForge MCP (completes 08-22 entry):** user connected the InsForge MCP mid-session → `run-raw-sql` access. **Schema discovery first (read-only information_schema): InsForge `auth.users` uses `email_verified boolean`, NOT Supabase's `email_confirmed_at`** — SQL artifact corrected before deploy (both functions now test `email_verified = true/false`; `created_at` assumption held). Deployed `check_signup_email` + `purge_signup_ghost` via MCP in one call. Verified: classifier 3/3 live (`dranrebwenbalangbang@mabinicolleges.edu.ph`→in_progress, `balangbangb@gmail.com`→registered, fresh→none); purge executes (0 rows on nonexistent). **End-to-end ghost replacement proven on the user's real abandoned email:** re-registered via UI with new name/password/role → route purged the Aug-22 ghost (DB shows old created_at gone, single new row) → full wizard → `/otp` redirect. Registered-block: `balangbangb@gmail.com` retry → "Email already exists. Try signing in instead." on step 2, nothing fired. All five flows green.
- **2026-08-22 — Signup ghost replacement + registered-block (user-directed final design):** abandoned unverified signups no longer trap emails. `scripts/sql/signup-hygiene.sql` ships two SECURITY DEFINER functions (**needs ONE manual paste into InsForge Dashboard SQL editor — not yet deployed at log time**): `check_signup_email(p_email)` → `'none' | 'in_progress' | 'registered'` (registered = users row exists OR email confirmed) and `purge_signup_ghost(p_email, p_min_age text DEFAULT '10 minutes')` → deletes unconfirmed profile-less auth rows older than the floor. Route order: IP attempt-bucket (`SIGNUP_IP_LIMIT`, `sip:` key, threshold 10/min cap 15 min; **successful creation clears the whole bucket** so shared-network rollout weeks never trip it; skipped when no IP header) → classifier (`registered` → 409 "Email already exists. Try signing in instead." ON step 2) → time-boxed purge when `in_progress` → signUp. Duplicate-flavored InsForge errors map regex-style (/exist|duplicate|already/i) to the friendly *"A signup with this email just started…"* 409 — covers the <10-min fresh-ghost collision where purge correctly refuses to act. Net semantics: re-registering an unverified email older than 10 min replaces it wholesale — new name/password/role/dept ALL win; verified accounts untouchable; live sessions unhijackable. Dropped from earlier drafts: 24h bulk sweeper (demand-driven replacement suffices), resume branch + `resumed` flag (complete route untouched). Graceful degradation if functions absent: classifier errors → null → raw signUp path with regex-mapped messages (verified live pre-deploy). Verified live pre-SQL: fresh flow → /otp ✓, short-password flag ✓, duplicate → new friendly copy ✓, bucket 429 after ~10 probes with Retry-After ✓. Post-SQL verification pending user paste.
- **2026-08-22 — Signup errors surface on their own step; Back button removed (user direction):** wizard is now forward-only (top arrow = back to /login). **Account creation moved to step 2's Continue:** `POST /api/auth/signup` now takes only name+email+password and creates the InsForge auth user immediately, so duplicate-email ("User already exists") and weak-password errors from InsForge land on the email/password page instead of after role/department. Client adds an 8-char minimum gate (`MIN_PASSWORD_LENGTH`, stricter than backend — over-blocking only). New `POST /api/auth/signup/complete` receives {email, firstName, middleName, lastName, role, departmentCode}: dept resolve-or-seed + approver notifications moved verbatim from the old signup tail (name threaded through so notifications keep the real full name). Step-2 errors render via new `stepError` state (distinct from step-3's `apiError`), both cleared on input/step change; `pending_signup` sessionStorage write unchanged in timing (after complete succeeds). Abandon-at-step-3 leaves an unverified auth user without notification — same waste profile as the old abandon-after-submit. Verified live: short-password flag on step 2 ✓, duplicate flag on step 2 ✓, full flow → /otp redirect ✓, zero Back buttons ✓.
- **2026-08-22 — Signup split into 3-step wizard (user request):** `app/(auth)/signup/page.tsx` only — same AuthInput/AuthSelect/AuthButton components, zero API changes. Step 1 Name (first*/last*/middle), step 2 Account (email*/password*), step 3 Council (role + department). Every forward button reads **"Continue"**; final one performs the existing POST → sessionStorage `pending_signup` → `/otp` redirect unchanged. Per-step gating reuses the `submitted` red-border pattern (`STEP_REQUIRED` map); `submitted` resets on step change so borders never leak across steps; steps unmount conditionally while values persist in the single `form` state. Extras kept from plan: outline "Back" button beside Continue on steps 2–3 (flex gap-3 row). Departments fetch + fallback list untouched. **Same-day follow-up (user direction):** step indicator removed — card subtitle now reads "Request an account for your council." and the shell-level subtitle was dropped; form top-aligned via `AuthShell top` + inner `pt-4` wrapper (login-page pattern). Verified: `tsc --noEmit` clean, `next build` green. *(Back button since removed — see entry above.)*
- **2026-08-22 — Login lockout v2: cycle-based with password-reset redemption (supersedes the flat ladder below):** `lib/rate-limit.ts` email bucket now walks a 3-cycle state machine (`BucketConfig` gained a `mode: "cycles" | "ladder"` union; IP bucket keeps the ladder untouched): cycle 1 = 5 fails → 1 min cooldown, cycle 2 = 3 fails → 5 min cooldown, cycle 3 = 3 fails → **soft lock** whose cooldown rides `IDLE_TTL_MS` (24 h) — when the lock lifts, the staleness check wipes the bucket in the same request, so expiry can never leave a one-fail-from-relock zombie. Cycle counters reset to 0 on each cooldown trip; stale buckets (idle > 24 h) restart at cycle 1; success still clears everything. Route: `checkRateLimit`/`LockStatus` now carry `softLocked`, threaded through both pre-auth and post-failure 429 paths as a machine-readable `softLocked: true` JSON flag (error copy: "…locked for 24 hours. Reset your password to regain access."); `formatWait` gained an hours tier. Redemption loop: `/api/auth/change-password` calls `clearRateLimit("e:{email}")` after successful `resetPassword` (safe — completing it requires OTP + inbox access). UI: login page raises an inline soft-lock modal (registry overlay/card tokens, `role="alertdialog"`; Reset Password → `/forgot-password`, Try again later dismisses; overlay click closes). Verified: simulated-clock script drove the full 5→60s→3→300s→3→24h ladder + staleness/expiry/clear/IP-ladder regression (all passed), `tsc --noEmit` clean, `next build` green.
- **2026-08-22 — Login rate limiting (post-plan hardening):** `lib/rate-limit.ts` — in-memory two-bucket escalating lockout, zero new deps. Email bucket (`e:{lowercased email}`, threshold 5) and IP bucket (`ip:{xff first hop ?? x-real-ip}`, threshold 15; bucket **skipped entirely** when neither header exists rather than lumping everyone into one shared key). Ladder: `baseMs × 2^(failures − threshold)` capped (email 30 min, IP 1 hr); buckets expire 24 h after last failure; size-capped sweep at 5000. Route integration: pre-auth check → 429 + `Retry-After` before InsForge is touched; both 401 paths route through `handleFailedAttempt` which records failures and re-checks (a tripping failure returns the 429, not a 401); near-limit countdown on the last two allowed failures ("2 more attempts before your account is temporarily locked"); success clears only the email bucket (typos forgiven), IP bucket persists. 403s (pending/rejected accounts) never count — credentials were proven correct. Blocked requests never extend timers. In-memory accepted ceiling: resets on deploy, per-instance — DB-backed table is the documented upgrade path. No UI changes (client renders `data.error` verbatim). Verified: `tsc --noEmit` clean, `next build` green.
- **2026-08-09 — Profile pages for all roles (post-Step-29 refinement):** the sidebar popover "Profile" + all three mobile bottom navs already pointed at `/treasurer/profile`, `/adviser/profile`, `/admin/profile` — dead links that 404'd. Built them: three thin `force-dynamic` pages + one shared server `components/profile/ProfileView.tsx` — `requireRole(role)` → `users` select (first/middle/last name, email, role, account_status, `departments(name)` embed) → card with initials avatar circle (`h-14 w-14 rounded-full bg-accent-light text-accent`, first+last initials, `email[0]` fallback; middle name → single initial "F. M. Last") + role badge + divider rows (Email, Department — hidden for admin whose `department_id` is null — Account status). Read-only by design (no edit forms, no avatar upload — the `avatars` bucket stays unused). `LogoutButton` (client, best-effort `POST /api/auth/logout` → `router.push("/login")`, same as Sidebar) rendered `lg:hidden` — mobile logout sits at the page bottom (user direction: desktop keeps the sidebar popover). Verified: `tsc --noEmit` clean, `next build` green (24 routes, all three profile routes registered ƒ).
- **2026-08-09 — Step 29 Read-only views (Phase 11 done, all 30 features):** `readOnly` prop on the 4 dashboard/report components (mutating controls OMITTED, not disabled — distinct from the treasurer `canMutate=false` lock semantics); `RejectedEntryActions` leak fixed (gated on `canMutate`); proxy routes + `"admin"`; three new pages (adviser + admin event, admin report) reusing the treasurer dashboard components per ui-rules read-only convention; adviser home placeholder → read-only events list; admin dept Events/Reports tabs wired real data (mock props deleted). Admin RLS risk retired with evidence — dept-scoped tables are admin-unrestricted per `architecture.md` and proven by existing admin reads of `users`/`audit_logs`.
- **2026-08-09 — Post-Step-29 UI consistency:** adviser home + admin Events tab now mirror the treasurer home exactly — `EventCard` folder grid for Active Events, `EventListItem` rows grouped by year for Archive. Unblocked by an optional `href` prop on both components (default stays `/treasurer/events/{id}`; adviser/admin pass their own read-only paths). Admin `Event` row type extended (`num_entries`, `created_by_name`, `created_at`); motion stagger variants deduped into shared `lib/motion-variants.ts`. `tsc --noEmit` clean, `next build` green.
- **2026-08-09 — Search + view toggle for adviser/admin:** both surfaces moved to a shared `EventBrowser` client component (`components/events/EventBrowser.tsx`) — treasurer-style centered search bar (name filter, `useDeferredValue`) + `ViewToggle` grid/list + Active grid / Archive-by-year, all staggered. Plain inline input instead of treasurer's `?search=1` URL mode + FilterDropdowns (those depend on the treasurer-only searchable MobileTopBar and management filters). Admin `Event` row renamed `spent` → `total_spent` to match `EventBrowserItem`; duplicated EventsTab/groupByYear code deleted. `tsc --noEmit` clean, `next build` green.
- **2026-08-09 — Filters + Sort for adviser/admin (full treasurer parity):** `EventBrowser` grew the complete treasurer search mode — fake search bar → search screen (ArrowLeft exit, autofocus input) + Type/Treasurer/Date/Budget `FilterDropdown`s + "Search Results" divider, search-prompt empty state, no-matches EmptyState with "Clear filters", and a Sort dropdown on the Archive header. All filter/sort constants + logic copied from the treasurer client (`app/treasurer/home/client.tsx`); state-based instead of URL `?search=1` (treasurer's URL mode serves its searchable MobileTopBar, absent here). `tsc --noEmit` clean, `next build` green.
- **2026-08-09 — Step 25 Archive Event (history, consolidated from Current Status):** `POST /api/events/[eventId]/archive` — `requireRole("treasurer")`; guards → 404 (no event / wrong dept), 403 (no latest approved report), 409 (already archived or `has_unresolved_overspend`); flow: upload each signed page via `uploadSignedReport(report.id, i+1, file)` → `countPdfPages(getReportPdfBlob(reportId))` (regex on PDFKit uncompressed `/Type /Pages … /Count N`, fallback 1 — pdfjs-dist is NOT Node-safe, client-only) → `verifySignedDocument` (agent/document-verifier.ts: gpt-4o via OpenRouter, 3 attempts; checks fs_document_number + signature-like marks per `report_signatories` ordered by `sort_order` + page count — completeness only, never authenticity) → any fail: best-effort delete uploaded keys via `deleteSignedReportPage` + 422 with per-check `checks`/`summary` → pass: race-safe event update `.eq("id").eq("status","open")` (0 rows → 409 + rollback), report update `{signed_document_urls: [keys], signed_page_count, signing_confirmed_by/at}`, audit `event.archived`, revalidate ×3. UI: `ArchiveEventModal.tsx` — `ArchiveEventButton` (outline destructive `border-error text-error`) + 3-phase modal (upload with "every page, not just the signature page" notice + multi-image thumbnails; uploading spinner; result screen `bg-success-lightest`/`bg-error-lightest` per-check rows — stays open on failure, Try Again resets, Done closes + `router.refresh()`); dashboard gate `canArchive = latestReport?.status === "approved" && !isArchived && !event.has_unresolved_overspend`. Verified: tsc clean, build green.
- **2026-08-09 — Step 21e pdfjs-dist SSR crash (history):** pdfjs-dist is NOT Node-safe — `DOMMatrix` referenced at module scope (confirmed `node -e "import('pdfjs-dist')"` → `DOMMatrix is not defined`); client components are SSR-evaluated, so the static value import crashed every SSR of `/treasurer/reports/[eventId]`. Fix in `components/reports/PdfViewer.tsx`: static value import → `await import("pdfjs-dist")` inside the render effect; `GlobalWorkerOptions.workerSrc` from the same `WORKER_SRC` module constant; `import type` kept (erased at compile).
- **2026-08-09 — Step 21d signatory wrap (history):** `ReportPdf.tsx` signatory columns wrap at 3 per row — ≤3 keep equal `flex:1` spread; >3 use `flexWrap:"wrap"` + `width:"33.33%"` columns with `marginBottom:12`. Verified on a 5-signatory report (row1 = 3 names y≈329, row2 = 2 names y≈280).
- **2026-08-09 — Steps 15–16 residuals (history, consolidated from Current Status, all still true):** `discardReceiptEntry` is server-guarded (fire-and-forget from UI; `is_locked` check added so all entry mutations block during pending/approved reports — same parity as deduct/void); `treasurer_reviewed` enum member exists but is unused — the authoritative transition is `ai_parsed → deducted`; orphaned receipt blobs for discarded entries are unreachable (keyed by the deleted entryId) — cleanup deferred; the overspend callout is duplicated between `LogEntryModal` and `ReceiptReview.tsx` — dedupe recorded as debt.
- **2026-08-09 — Phase 10 (Notifications) complete:** single `createNotification` choke point replaces 7 inline `notifications.insert` sites; content map in `lib/notifications.ts` drives push AND list pages (3 new types: `report_approved`, `manual_entry_pending`, `entry_rejected`); badge count passed server-side from layouts (deviates from plan's client-polling provider — refresh-on-navigation, per ponytail); `PushSubscriber` never prompts for permission (browser settings are the entry point); retention job is a scheduled InsForge function — SQL artifact + boundary check shipped, actual scheduled-function deploy needs MCP.
- **2026-08-09 — Step 28 Audit Log (Phase 11 start):** the Audit Logs tab already had UI (rendering empty `mockAuditLogs`) — wired real data instead of building the planned `/admin/departments/[deptId]/audit-logs` route: page fetches `audit_logs` scoped to dept (limit 100, desc), actor names resolved via second query on distinct actor ids (`.in()` guarded — empty array errors on PostgREST); target id short form `#id.slice(0,8)`; expandable metadata = plain chevron toggle + `<pre>` pretty JSON (no separate modal); timestamp gained time-of-day. Doubles as the plan's end-to-end audit check: 20+ audit insert sites exist across Phases 2–9.
- **2026-08-08 — Overspend stamp needs `is(..., null)`, not `eq(..., null)`:** InsForge's PostgREST proxy rejects `eq.null` (400) — only `.is(col, null)` works. Also: never trust an unchecked count query; `{ data }` without `error` silently degrades to 0 and skips critical logic.
- **2026-08-08 — Destructive buttons are outline-only project-wide:** Confirm Rejection was built filled-red (`bg-error text-white`) in Phase 8, contradicts ui-rules — fixed to `border border-error text-error hover:bg-error-lightest`. Reject trigger already conformed.
- **2026-08-08 — `report_rejected` notification is best-effort:** fires only when the dept has an `active` treasurer; dev data has none (and `users` reads are RLS-hidden to advisers) so it skipped — guard clause, not a bug.
- **2026-08-08 — FS number anchored per EVENT:** report number = first-assigned `fs_document_number` (revision = max+1); suffix increments per event via dept/year counter; cancelled/rejected regens never consume a new number.
- **2026-08-08 — Cancelled reports hidden from treasurer list:** event whose newest report is `cancelled` → back to "No report yet" (PDF still viewable on detail page); rejected stays in "With reports". Stack ordered newest `generated_at` first.
- **2026-08-08 — Real cancel + persistent ReportViewer (supersedes Step 20 mock):** cancel = treasurer, dept-scoped, event not archived, `pending_adviser_approval` only, race-guarded (racing approve → 409); PDF always viewable via server-side viewer (survives logout/login); no adviser cancel notification.
- **2026-08-08 — Step 20 real generation (incl. RLS discovery):** no `router.refresh()` after generate (kills client preview); cross-user notification inserts were silently failing → added `notifications_same_dept_insert` RLS policy; `pdf_url` = storage key not URL (proxy streams); `reports` orders by `generated_at` (no `created_at`).
- **2026-08-07 — Step 19 signatories + mock preview:** signatories client-only (localStorage) until Step 20 persists `report_signatories`. Note: codebase uses `insforge.database.from(...)`, not `insforge.from(...)`.
- **2026-08-07 — Count inputs block letters:** retired `type="number"` (allows `e`/`E` → NaN→0) for `FloatingInput.digits` (strips non-digits) + `inputMode="numeric"`; tradeoff: no spin buttons.
- **2026-08-07 — Currency inputs digits-only + live separators (reverses 08-03 trade-off):** live `formatNumberInput` + comma-aware `toNumber()` at every coercion point; decimals survive.
- **2026-08-05 (final) — Withdraw/discard semantics (supersedes the soft-terminal `discarded` entry below):** nothing irreversible → removable: `ai_parsed` → Discard = hard delete; `pending_approval` manual → Withdraw = hard delete; `rejected`/`resubmitted` → permanent (resubmit only, 2nd rejection terminal); `deducted` → Void. `discarded` no longer reachable.
- **2026-08-05 — Discard consolidated (SUPERSEDED by above):** kept for history — `deleteReceiptBlob` + conditional-delete race-guard work retained.
- **2026-08-04 — Void moved into the entry info modal (user direction):** full-width destructive pill at modal bottom when `canMutate && status==="deducted"`; stale modal closes on success.
- **2026-08-03 — "Entry has no event" on approve (fixed):** PostgREST to-one embeds return a single **object**, not an array; extract shape-agnostically.
- **2026-08-03 — Adviser batch-approve did nothing (fixed):** confirm dialog dispatched `executeUserAction`, which no-ops without `userId`; batch path now dispatches `executeBatchApprove`.
- **2026-08-03 — Rejection reason visible to treasurer:** `rejection_reason` threaded to EntryDetailModal red callout.
- **2026-08-03 — Manual entries get real titles:** `entryTitle(e)` = supplier → "Category — detail" (recipient/route/occasion/item) → category → "Untitled entry".
- **2026-08-03 — Manual currency/number bugs (user-reported):** store raw strings while typing, coerce at edges only; dropped live separators (reversed 08-07).
- **2026-08-03 — Step 16 Manual entry submission:** rows `type:"manual"`, `status:"pending_approval"`, never deducted at creation; per-form per-category gate `max(minCeiling, round(budget×pct/100))`, explanation never blocks; floors: Transportation 20%/₱300 · Meals 55%/₱500 · Honorarium 45%/₱1,000 · Supplies 25%/₱500 · Printing 12%/₱300 · Rental 65%/₱1,000 · Others 20%/₱300.
- **2026-07-12→13 — Landing page:** token-only per Phase 1; Figma rebuild (structure only, Montserrat→Poppins); Antigravity hierarchy (whitespace exceeds 24/32px caps — pre-approved); hero image holder.
- **2026-07-13 — Phase 0 (Route × Role × Precondition Matrix):** `docs/auth-matrix.md` + `lib/auth-guard.ts` `requireRole(role, departmentId?, precondition?)` — the server-side security boundary.
- **2026-07-13 — `01 Auth Shell` (UI only):** 5 `(auth)` pages, mock submits; real auth = 03/04.
- **2026-07-15 — Forgot-password 3-step flow wired:** `/forgot-password` → `/otp?purpose=reset` → `/change-password` → `/login`; back buttons via `backHref`.
- **2026-07-17 — `03 Auth` complete:** 5 API routes (signup w/ dept code→ID + auto-seed + notification routing, otp/send anti-enumeration, otp/verify, change-password); dept auto-seeds on first signup (Phase 2 replaces).
- **2026-07-16 — `02 Database Schema` complete:** 11 tables; one active adviser/treasurer per dept (partial unique indexes); derived columns never stored; RLS on all tables; storage access is app-level path-based, not SQL RLS.
- **2026-07-17 — `04 Login/session/redirect` complete:** login checks `account_status` → role redirect; middleware + route-group layouts (note: `middleware.ts` deprecated vs Next 16 `proxy.ts`).
- **2026-07-17 — `05 Admin Departments` (UI):** mock data only; Phase 2 `06` wires real.
- **2026-07-18 — Animation stack:** CSS → framer-motion (micro) → GSAP (heavy); primitive order shadcn → @base-ui → custom.
- **2026-07-18 — Signup bug fix:** SSR `signUp` returns optional `user` (no false 500); users row deferred to create-profile after OTP; SSR client's `verifyEmail()` doesn't persist cookies (see below).
- **2026-07-18 — Auto-redirect + welcome email on approval:** `/api/auth/status` polling (10s) → redirect/banner; nodemailer welcome email on approve.
- **2026-07-18 — OTP session cookie gap fixed:** manually persist access/refresh cookies in OTP verify (SSR client never writes response cookies).
- **2026-07-18 — Phase 3 (Adviser Approvals) complete:** 4 server actions with full precondition chains; native button/checkbox (no shadcn init). [CORRECTION 08-03: "events join returns array" was wrong — to-one embeds return objects.]
- **2026-07-18 — Entry logging → modal/sheet:** LogEntryModal (web modal / mobile sheet); standalone page kept as fallback.
- **2026-07-18 — Phase 5 Step 13 (Entry Logging UI):** upload / manual / review mock flow.
- **2026-07-18 — Phase 4 (Treasurer Events & Budget) complete:** `lib/queries/events.ts` computes `total_spent`/`budget_locked`/`is_locked` at query time — never persisted.
- **2026-07-25 — New Event → modal/sheet:** NewEventModal; standalone page deleted.
- **2026-07-29 — Manual entry UX refined:** 2-step flow (category picker → quick form); 7 categories in `manual-categories.ts`; localStorage witness reuse.
- **2026-08-01 — Receipt OCR: OpenRouter → Google Gemini direct (speed):** 60–90s → ~2.5s via `lib/gemini.ts` (`gemini-3.5-flash-lite`, JSON mode, 120s timeout).
- **2026-08-01 — Receipt upload downscaled client-side:** ≤1600px JPEG q0.8 (5–10× smaller; falls back on any failure); accuracy re-verified.
- **2026-08-02 — 3-outcome guided upload (valid/borderline/invalid):** verdicts short-circuit with guidance + audit, no row; only `valid` creates a row; numberless receipts → `""` not null; verdicts don't count toward the 3-attempt ceiling.
- **2026-08-01 — Model pinned `gemini-3.5-flash-lite`:** benchmark — faster + tighter spread than 3.1-flash-lite, 9/9 accuracy.
- **2026-08-02 — EntryDetailModal scrollbar hidden:** plain CSS class (not `@utility` — Turbopack didn't compile it).
- **2026-08-02 — Camera UX (4 iterations):** fullscreen viewfinder portal to body (parent transforms break fixed); flash only when torch capability; green sweet-spot brackets + live dim/close/far/blur hints; thumb-comfort sizing; mobile-only, no FAB; real-device QA outstanding.
- **2026-08-03 — Expense category at parse time:** AI infers (7-way taxonomy, doubt → `others`), stored on insert; AI final — review read-only; no backfill.
- **2026-08-01 — EventCard Figma "folder 2" + refinements:** all info on paper sheet, framer-motion hover, grid capped 5 cols, neutral hover only, mobile filter popover; mock data removed (breakdown deducted-only; card always rendered).
- **2026-08-30 - Notification gaps filled (adviser overspend + treasurer entry-approved):** two new createNotification types via lib/notifications.ts notificationContent - event_overspend (adviser, title 'Budget overrun', -> /adviser/events/[id]) and entry_approved (treasurer, -> /treasurer/events/[id]). Web Push automatic (the createNotification choke point already calls sendPushToUser). Fires: (1) event_overspend in confirmReceiptEntry (actions/entries.ts) and in batchApproveEntries backstop (actions/approvals.ts) on the TRUE deducted crossing only - NOT the provisional pending-submit flag, so a rejected/withdrawn pending entry never sends a false alert. Once-per-event is guaranteed by the durable causes_overspend flag + entryCausesOverspend returning false once already over, so at most one adviser push per event. (2) entry_approved inside batchApproveEntries' per-entry audit loop to entry.created_by (added created_by to that select). Reject-report (report_rejected) and reject-entry (entry_rejected) pushes to the treasurer were ALREADY firing - no new code. The treasurer 'event approved' leg was already report_approved on report approval. No schema, no pages, no RLS, no UI changes; UI list + badge render the new types via the shared content map.
- **2026-08-30 - Web Push never delivered (root-caused + fixed):** `push_subscriptions` was EMPTY (schema rows:[]) because nothing ever called `Notification.requestPermission()` - `PushSubscriber` bailed whenever `permission !== "granted"`; no prompt existed, so `pushManager.subscribe()` never ran and there was no subscription for `sendPushToUser` to deliver to. Server was fine (all VAPID keys in Vercel prod); the break was purely client-side. Fix: extracted browser helpers to `lib/push-client.ts` (`ensurePushSubscription()` = get-or-create + sync, shared); `PushSubscriber` keeps the silent already-granted re-subscribe; NEW `components/notifications/PushEnableToast.tsx` - first-login toast (once per browser via `liquifi_push_prompt_dismissed` localStorage), shown only when `permission === "default"`, its "Enable" button is the user-gesture `requestPermission()` trigger -> on grant calls `ensurePushSubscription()`; "denied" shows a settings-link message instead. Mounted in both treasurer + adviser layouts. No server/schema/route changes. `tsc --noEmit` clean, `next build` green. NOTE: real delivery still needs the user to click Enable + Chrome running in background; iOS Safari has no Web Push.
- **2026-09-04 - Receipt parse reverted to single-request fast path (supersedes the two-phase v1).** The two-phase upload fixed the 10s Vercel ceiling but added a poll loop that regressed perceived time to ~20s (3 attempts x Gemini + 1.5s sleeps; 1280px resize also made Gemini borderline/error more). User confirmed the pre-two-phase single request felt faster and set a 2-4s target. New design keeps ONE awaited request (not a background worker): `app/api/entries/receipt/route.ts` now uploads -> inserts provisional `pending_ai_parse` row -> runs Gemini INLINE -> promotes to `ai_parsed` and returns the parsed receipt in one round trip (~2-4s clean). To stay under the ceiling safely: `lib/gemini.ts` `TIMEOUT_MS` 120000 -> 8000 (a hung model fails fast instead of blowing the request); `lib/image.ts` `MAX_DIM` 1280 -> 1600 (restores legibility -> fewer borderline/error retries; corrected the misleading "fewer tokens = faster" comment - the model floors on a single image). `[entryId]/route.ts` is KEPT but demoted to a retry-only handler: on a transient inline `{status:"parsing"}` the client re-parses the SAME provisional row from its stored blob (no image re-upload, no duplicate blob). `ReceiptUpload.tsx` `handleUpload`: happy path = one POST -> parsed; only on `parsing` does it call `onPending(entryId)` + bounded reparse retries (MAX_ATTEMPTS=3, RETRY_DELAY_MS=900) against `[entryId]`, then manual-entry fallback on exhaustion. Verdicts/duplicate still delete row+blob on both routes. `tsc --noEmit` clean, `next build` green. NOT yet deployed.
---

## Notes

- **2026-08-30 Manual entry detail modal - per-category trip info:** the manual-form details block now renders a "Details" section that drives off `CATEGORIES[category].fields` (from `form_payload_json.field_values`), so every category shows its computed inputs under the right label: Transportation (Fare, Passengers, Trip Type round-trip/one-way + one-way ride count when round-trip), Meals (Per-head Rate, Headcount), Honorarium (Amount), Printing (Rate/page, Pages, Copies), Rental (Daily Rate, Days). Supplies/Others rely on the existing item breakdown. Free-text (Route, Recipient, Occasion, Witness, Purpose) still shown below. New `manualSpecRows()` helper in `EntryDetailModal.tsx`; currency fields format via `formatPHP`, numeric via `toNumber`.
- **2026-08-30 Manual entry title + detail (user tweak):** `entry-title.ts` now returns just the free-text detail (recipient/route/occasion/first line item) for manual entries instead of `Category - detail` - so "Transportation - sm to manila" shows as just "sm to manila" (category prefix dropped; still falls back to the bare category label when no detail exists). `EntryDetailModal` now renders a manual-form details block from `form_payload_json` (`formPayload`): Recipient, Route, Occasion/Purpose, Witness, and Purpose - every field the treasurer filled out. No schema change; `formPayload` was already persisted/selected.
- **2026-08-30 Multi-image manual entries (see also Decisions deb storm):** up to 4 photos per manual entry. `image_url` is a JSON array string when N>1, bare key when N=1 (legacy-safe, no schema change - `text` column). New web-safe module `lib/image-keys.ts` holds `parseEntryImageKeys` on purpose: `lib/storage.ts` pulls the server SDK so client components can't import it. `uploadReceipt(..., index)`, `getReceiptBlob(entryId, index)`, `deleteReceiptBlob` resolve arrays. Read route `GET /api/entries/[entryId]/image?i=N`. Manual photo route reads `index` + caps at 4. `ManualQuickForm` collects `photoFiles[]` and submits `imageKeys[]`; `submitManualEntry` stores array/single. `EntryCard` thumbnail shows first image for manual + receipt; `EntryDetailModal` 2-col clickable grid + `ImageViewer` paging. Cap is `MAX_PHOTOS`/`MAX_IMAGES_PER_ENTRY=4` in both client and server - raise both together.

*(Environment quirks, sandbox limitations, verification commands that passed/failed, and anything a future agent needs to know before touching this codebase again. Append, don't overwrite.)*

- **SDK import correction (flagged):** `architecture.md` imports the server client as `from "@insforge/ssr"`. The installed package is `@insforge/sdk@1.4.4` (per AGENTS.md), which exposes the server client at the subpath `@insforge/sdk/ssr` (`createServerClient`). There is no standalone `@insforge/ssr` package installed. `lib/insforge-server.ts` uses `@insforge/sdk/ssr`. Corrected import path only — API matches the architecture's intent. If a future session adds `@insforge/ssr` separately, this should be reconciled.
- `users`-table lookup in `getCurrentUser()` (auth-guard) requires the `users` table from Phase 1 / `02` to exist before runtime auth works. The guard compiles now and is structurally correct; it becomes live once schema + signup land.
- **InsForge storage RLS:** InsForge manages storage access internally — SQL-level `storage.objects` policies (like Supabase) are not available. Storage buckets are created as public; access control is enforced application-side via path-based conventions (`departments/{deptId}/events/{eventId}/receipts/{entryId}.jpg`) and server-side auth guards in API routes.
- **2026-09-04 Schema fix: `entries.status` IS constrained despite AGENTS.md claiming it's free text.** Deployed two-phase upload failed with `23514 ... violates check constraint "entries_status_check"` ("Failed to save the upload."). `ALTER TABLE entries DROP/ADD CONSTRAINT entries_status_check` to add `'pending_ai_parse'` to the allowed set (previously: draft, ai_parsed, treasuser_reviewed, pending_approval, approved, rejected, resubmitted, discarded, voided, deducted). While at it: `entries.type` is also CHECK-constrained to `receipt|manual`. The `get-table-schema` MCP list did NOT surface CHECK constraints (only columns/indexes/FKs/RLS), so the check had to be read via `pg_constraint`. AGENTS.md "status is unconstrained text" is wrong — treat the CHECK constraint as authoritative. No app redeploy needed; the deployed code was correct, the schema was stricter than documented.
- **2026-09-04 Deployed two-phase receipt upload (function-timeout fix) to prod.** Root cause: `POST /api/entries/receipt` ran Gemini inline and blew past Vercel Hobby's 10s ceiling ("Could not reach the server..."). Now Phase 1 (`app/api/entries/receipt/route.ts`) uploads + inserts a provisional `status:"pending_ai_parse"` row and returns `202` immediately (no Gemini); Phase 2 (`app/api/entries/receipt/[entryId]/route.ts`, new) does the AI parse on a separate request the client polls (`ReceiptUpload.tsx` `handleUpload` loop, `onPending` prop, `POLL_DELAY_MS=1500`, `MAX_ATTEMPTS=3`). Verdicts (invalid/borderline/multiple) delete the row+blob and return `422`+code; dup returns `409`; parse-throw returns `{status:"parsing"}` for retry; valid entry promotes to `ai_parsed`+`ocr_raw_json`. `actions/entries.ts` `discardReceiptEntry` also guards `pending_ai_parse` so closing the modal mid-poll cleans the leaked row; `entries/new` page gets a no-op `onPending` (full page, no close-cleanup). Verified: `tsc --noEmit` clean, `next build` green. Deployed via `vercel --prod` (+49s build), re-aliased `liquifi-app.vercel.app`, curl `/login` = HTTP 200.
- **2026-09-04 Deployed single-request receipt fast path to prod.** Deployment `liquifi-j86stymp0-diamondblack488-7599s-projects.vercel.app` (build 33s, ready 52s; auto-aliased `liquifi-blond.vercel.app`). Re-aliased `npx vercel alias set liquifi-j86stymp0-... liquifi-app.vercel.app` → `Success!` (the stderr banner Vercel prints is normal; check the `Success!` line). Smoke: `curl /login` = HTTP 200 + full Liquifi HTML; `POST /api/entries/receipt` with empty body returns HTTP 500 (expected — unauthenticated + no form body hits the guard/validation path, proving the route is deployed, not a 404). Supersedes the 2026-09-04 two-phase deploy note above — still redeploy + re-alias after every later `vercel --prod` (manual alias does not auto-follow).
- **Canonical production URL is `https://liquifi-app.vercel.app`** (user-designated 2026-08-25). The project's auto-updating production domain is `liquifi-blond.vercel.app` (Vercel-generated default); `liquifi-app.vercel.app` is a MANUAL alias that does NOT follow `vercel --prod` deploys — it sat frozen on an 11-day-old build until re-pointed (`vercel alias set <prod-deployment> liquifi-app.vercel.app`). After every production deploy, either re-run that alias command or (permanent fix, one dashboard click) add `liquifi-app.vercel.app` as a Production domain under Project → Settings → Domains so it auto-updates with blond. Always quote `liquifi-app.vercel.app` to the user.

---

## How to Update This File

After finishing a feature:

1. Check its box under **Progress**.
2. Update **Current Status** — `Last completed` becomes what you just finished, `Next` becomes the next unchecked item in build order, `Phase` updates if you crossed a phase boundary.
3. Add one bullet to **Decisions Made During Build** describing what was actually built (file paths, exact function/table names, any deviation from `build-plan.md` or `architecture.md` and why).
4. Add to **Notes** only if something environment/verification-related happened (build failures, sandbox network issues, lint warnings carried forward).
5. Never delete history from this file — it is append-only except for the **Current Status** and checkbox sections.
6. If a decision here ever contradicts `architecture.md`, the architecture doc wins — flag the conflict in **Notes** rather than silently resolving it.
