# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 11 — Audit & Read-Only Views (COMPLETE — all 30 build-plan features done) + post-plan hardening
**Latest completed:** 2026-09-12 Budget-proof overhaul: multi-image proofs (up to 5 photos, every photo must parse as a valid budget document, claim matches if ANY one photo extracts it within ₱0.01), initial-budget proof mismatch now REJECTS the event (full rollback — event row deleted, blobs removed, "the event was not created" in every failure message), and tappable budget-history cards opening a detail sheet (shared `ImageViewer` extracted to `components/ui/ImageViewer.tsx`, new `BudgetProofDetailModal` + client `BudgetHistoryList`). `app/api/proofs/route.ts` rewritten with upload-verdict-transient-mismatch-insert branches, `rollbackUploads`/`rollbackInitialEvent` helpers, audit rows per branch; storage keys now `${deptId}/events/{eventId}/proofs/{proofId}-{index}.jpg` (legacy bare keys read fine); proofs image proxy takes `?i=` index. `tsc` + `next build` green.
**Prior completed (chain):** 2026-09-09 treasurer UX pass: All Events page gained a 24px top margin (`pt-6` on `app/treasurer/events/client.tsx` root, page-scoped); treasurer Home page now has the same staggered FadeIn entrance as All Events across all five event surfaces (mobile/desktop active grids, mobile/desktop archive, search results) via the shared `components/ui/FadeIn.tsx` (`delay={30 + index * 80}`, keys on the wrapper). `tsc --noEmit` green. Prior: 2026-09-08 event-navigation instant-open work: all four event-detail link primitives (`EventCard`, `FolderCard`, `EventListItem`, `ArchiveEventRow`) now full-prefetch their event route via `prefetch` (Next's viewport-first scheduler warms visible links lazily); `staleTimes.static` raised 30s → 300s (5-min snapshot for fully prefetched routes); `EventLiveRefresh` runs one silent `router.refresh()` 500ms after mount so a cache-served page reconciles fresh server data in place (ref guard + 2s throttle, no loop, realtime stays authoritative). Build green. Supersedes for event-detail links the 09-07 "no passive prefetch, intent-only" stance (NavItem + Home View-all links unchanged).
**Next (non-feature):** E2E smoke test of the read-only views + EventBrowser + profile pages (needs admin/adviser creds); verify/create the `push_subscriptions` RLS policy; deploy the notification-retention scheduled function (last two need MCP).
**Open items / debts flagged:** real-device phone re-verify of sheet MID slow-release path; `/sync` for AGENTS.md outcome list; audit-log filters are client-side over a `.limit(100)` fetch (add server-side pagination when a dept exceeds 100 logs); overspend callout duplicated between `LogEntryModal` and `ReceiptReview.tsx` (dedupe); sweep remaining SECURITY DEFINER functions for PUBLIC EXECUTE (see 08-31 entry).
**Status paragraph:** All 30 features done (checkboxes below reflect it). Step 29 shipped the read-only layer: `readOnly` prop on `BudgetSummary`/`EventDashboardActions`/`SpendingBreakdownCard`/`ReportViewer` (mutating controls OMITTED, never disabled), `RejectedEntryActions` gated on `canMutate` (adviser leak fixed), proxy routes (image + pdf) now allow `"admin"`, three new pages — `/adviser/events/[eventId]`, `/admin/departments/[deptId]/events/[eventId]`, `/admin/departments/[deptId]/reports/[eventId]` — reusing the treasurer dashboard components, "View event" link in the adviser review header, admin dept Events/Reports tabs wired to real data (mock props deleted). Post-step refinements: `EventCard`/`EventListItem` gained an optional `href` prop (treasurer default `/treasurer/events/{id}` unchanged); shared `components/events/EventBrowser.tsx` gives adviser home + admin Events tab the full treasurer look — fake search bar → search mode (ArrowLeft exit, autofocus input) with Type/Treasurer/Date/Budget `FilterDropdown`s + "Search Results" divider, search-prompt + no-matches ("Clear filters") empty states, `ViewToggle` grid/list on Active, Archive grouped by year + Sort dropdown — state-based search, not the treasurer URL `?search=1` mode (that URL dance exists for the treasurer-only searchable MobileTopBar). Commit only when explicitly requested (`.dev-err.log`, `.dev-out.log`, `figma/` are untracked junk).

---

## Progress

### Phase 0 — Authorization Foundation
- [x] 00 Route × Role × Precondition Matrix

### Phase 1 — Foundation
- [x] 01 Landing + Auth Shell
- [x] 02 Database Schema
- [x] 03 Auth — Signup, OTP, Approval Routing
- [x] 04 Login, Session, Role-Based Redirect

### Phase 2 — Admin: Departments & Approvals
- [x] 05 Admin Departments Page — Full UI
- [x] 06 Admin Departments — Real Data + Mutations
- [x] 07 Admin Approvals — Adviser Signups

### Phase 3 — Adviser: Approvals
- [x] 08 Adviser Approvals Page — Full UI
- [x] 09 Adviser Approvals — Real Data + Mutations

### Phase 4 — Treasurer: Events & Budget
- [x] 10 Treasurer Home + Event Creation — Full UI
- [x] 11 Event Creation + Budget Lock — Real Data
- [x] 12 Event Dashboard — Full UI

### Phase 5 — Treasurer: Logging Entries
- [x] 13 Entry Logging — Full UI
- [x] 14 Receipt Parsing — OpenRouter Integration
- [x] 15 Receipt Confirm / Discard + Budget Deduction
- [x] 16 Manual (No-Receipt) Entry Submission

### Phase 6 — Voiding & Overspend
- [x] 17 Void Entry — Full UI + Logic
- [x] 18 Overspend Explanation Flow

### Phase 7 — Reports: Generation
- [x] 19 Report Generation — Full UI
- [x] 20 Report Generation — Real Logic
- [x] 21 Cancel Pending Report

### Phase 8 — Reports: Adviser Review
- [x] 22 Adviser Report Review — Full UI
- [x] 23 Report Approve / Reject — Real Logic

### Phase 9 — Archiving
- [x] 24 Archive Event — Full UI
- [x] 25 Archive Event — Real Logic

### Phase 10 — Notifications
- [x] 26 Push Notifications — Full Setup
- [x] 27 Notification Retention Job

### Phase 11 — Audit & Read-Only Views
- [x] 28 Audit Log — Admin View
- [x] 29 Adviser & Admin Read-Only Event/Report Views

**Total: 30 / 30 features complete**

---

## Decisions Made During Build

### 2026-09-12 - Budget History read path (BudgetProof feature, UI slice)

- **Figma-directed UI slice:** added a full-width white **Budget History** card below the two CTAs on the treasurer event dashboard (Figma 171:212; `EventDashboardActions.tsx`, mobile-only - the desktop CTA frame is `lg:hidden` on that route). Nothing else on the event page changed.
- **New route:** `app/treasurer/events/[eventId]/budget-history/page.tsx` (Figma 142:199) - back arrow, centered History title, proof cards grouped by month, `formatPHP` amount + "Submitted on ...". Server component: `requireRole("treasurer")` + explicit cross-department `notFound()` guard (belt-and-suspenders on top of RLS).
- **New table `budget_proofs`** (`scripts/sql/budget-proofs.sql`, applied via MCP raw SQL): `event_id` FK CASCADE, `department_id`, `uploaded_by -> auth.users`, `uploaded_at`, `type` CHECK `initial|increase`, `claimed_amount`, `proof_url` (JSON-array storage keys, nullable), `ai_extracted_amount`, `verification_status` CHECK `pending|matched|mismatch`, `resulting_budget_total` with CHECK (matched implies non-null total). RLS mirrors every dept-scoped table: `get_user_role()/get_user_department_id()` helpers, dept policy for adviser+treasurer, admin-unrestricted policy.
- **Query:** `lib/queries/budget-proofs.ts` - `getBudgetProofsByEvent(eventId, departmentId)`, React-cached like `lib/queries/events.ts`, newest first. No uploader-name join (UI doesn't show it); add only if cards need attribution.
- **Deferred (separate write path, not in this slice):** Gemini verification POST API + `agent/budget-proof-parser.ts`, `budget-proofs` storage bucket + image proxy, adviser-page equivalent button, desktop Budget History entry (desktop CTA lives in `BudgetSummary` - left untouched per "don't change the event page UI"). Table is additive/empty until the write path lands; UI renders seed rows if any.
- **Verification:** `npx tsc --noEmit` green.

*Condensed 2026-09-07 on request — full verbatim history preserved in git. Newest first. Superseded entries are marked [SUPERSEDED] with the replacement named.*

### 2026-09-10 - Treasurer event-report guided workspace

- **Option A selected in visual brainstorming:** `/treasurer/reports/[eventId]` now uses one centered `max-w-4xl` flow instead of the crowded desktop 3/5 report + 2/5 spending dashboard. The hierarchy is compact event header → three labeled progress rails → `bg-surface-inverse` state-aware next-action workspace → latest report file → collapsed secondary information. `lib/report-workspace.ts` deterministically maps no-report/rejected/cancelled/pending/approved/archived to the active step; `scripts/check-report-workspace.ts` covers all six cases.
- **Progressive disclosure, no new dependency:** Spending totals/category bars and immutable previous revisions remain available through native `<details>/<summary>` sections, closed by default. Existing queries, department guard, report ordering, `ReportViewer`, `ReportFileCard`, PDF proxy, generation/cancel actions, and adviser/admin report pages are unchanged. `ReportGenerationFlow` + `SignatorySetup` were restyled for the dark workspace without changing props or localStorage reuse.
- **Verification:** no-report, rejected (including 11-history disclosure), and approved states checked on 1440px desktop and 390px mobile; disclosure interaction is keyboard/click accessible and the browser console is clean. `npx tsx scripts/check-report-workspace.ts`, scoped ESLint, and `npx tsc --noEmit` pass.

### 2026-09-09 - Treasurer + Adviser reports index redesign

- **Shared Figma-derived index:** `components/reports/ReportsOverview.tsx` now renders both `/treasurer/reports` and `/adviser/reports`; role pages remain Server Components and only map their department-scoped data into serializable `ReportOverviewItem`s. Treasurer featured folders are approved/open events under **Ready for Signing**; Adviser folders are pending/open reports under **Pending Reports**. The dark **Action Required** banner is Treasurer-only and appears only when the latest report for an open event is `rejected`; it uses the supplied `public/mascot.json` through `LottiePlayer` and links directly to that event's report detail.
- **Responsive hierarchy:** mobile follows Figma node `150:137` (featured 132px folder cards, filter strip, compact rows, archive list); desktop uses the same hierarchy with a max-width shell and two-column Reports/Archive layout. The mobile header stays visible on the two report indexes and the All/Pending/Approved/Rejected strip uses native CSS `sticky top-16` beneath it (`lg:top-0`), so both controls remain reachable; filters are client-only and preserve existing report state/data rules. Pure sorting/grouping is in `lib/report-overview.ts`, checked by `scripts/check-report-overview.ts`.

### 2026-09-09 - void modal re-submission bug (unstable entry ref) [SUPERSEDED → option B auto-close]

- **`VoidEntryModal` could be re-submitted after a successful void** (`components/entries/VoidEntryModal.tsx`): the reset effect keyed on `[open, entry]`, but the parent (`EntryList.tsx`) passes `entry` as an inline object literal rebuilt every render (`{ id, amount, label }`). The post-void `router.refresh()` re-render gave `entry` a new reference → the effect re-ran → `setDone(false)` flipped the terminal "Entry voided / Done" screen back to the form (reason still populated, submit re-enabled). A second submit then hit the server guard "Only deducted entries can be voided". Fix: key the reset effect on the stable `entry?.id` instead of the object ref (same class as the `EntryDetailModal` latch pattern). `tsc --noEmit` green.
- **Option B — auto-close on success** (user direction supersedes the option-A "keep success screen" fix): the success screen and `done` state are deleted entirely; `submit()` now just refreshes and calls `onSuccess()`. `EntryList.onSuccess` clears BOTH `selectedEntry` (detail sheet) and `voidTarget` (void modal), so the void modal closes itself immediately after a successful void — the reason form can't linger and re-submission is impossible. The `entry?.id` reset-effect fix is kept (still correct; reset on a new target). `CssBottomSheet` keeps the app's exit slide-down animation. Committed + deployed 2026-09-09.

### 2026-09-09 - treasure events page spacing + home entrance animation

- **24px top margin on All Events page** (`app/treasurer/events/client.tsx`): root container gained `pt-6` (24px) alongside the existing `pb-16`/`gap-5` — scoped to the page's own wrapper, NOT the shared `TreasurerLayoutShell`, so other treasurer pages keep their current top padding. Requested by user to visually separate the page top from the shell.
- **FadeIn entrance on treasurer Home page — option B (every card surface):** `app/treasurer/home/client.tsx` now wraps all five event-render maps in the same staggered `<FadeIn key={event.id} delay={30 + index * 80}>` used by All Events — mobile active `FolderCard` grid, desktop active `EventCard` grid, mobile archive `ArchiveEventRow` list, desktop archive year-grouped `EventListItem` list, and the search-results `ArchiveEventRow` list. Keys moved from the inner card to the `FadeIn` wrapper (needed for clean re-stagger on filter/search changes). Uses the shared `components/ui/FadeIn.tsx` (CSS entrance, reduced-motion safe). `tsc --noEmit` green.

### 2026-09-09 - folder pending dot removed

- **`FolderCard` red pending dot deleted** (user request): removed the `hasPending` prop + `bg-accent` dot span from `components/events/FolderCard.tsx`, its sole caller in `app/treasurer/events/client.tsx`, and the now-dead `has_pending` field in `lib/queries/events.ts` (return type + map + the `pendingEntryIds` Set / "awaiting adviser action" status check — the only consumer). Desktop `EventCard` never had a dot. `tsc --noEmit` green; git history has the code if the indicator ever returns.
- **`FolderCard` top-right dark slab removed** (follow-up): the light tab path ran its top edge at y=44.44 across the right half, exposing ~44px of the dark `#706D6D` body at the top-right corner ("black dot"). Reshaped the tab's right segment (`H338C346.284 44.4444 …V85Z` → `V12H333C341.284 12 348 18.7157 348 27H353V85Z`) so the top edge sits at y=12 across the full width, giving a uniform 12px rim symmetric with the left corner. Label slot preserved. Pixel-verified via canvas sampling; `tsc --noEmit` green.
- **`FolderCard` art reverted to Figma reference (2026-09-09, supersedes the slab fix above):** user review against `figma/mobile-folder/mobile-folder-idle.svg` + Figma node 57-32 confirmed the dark top-right region is the *designed* back panel above the tab's low plateau (tab top edge at y=44.44 to x=338, then arc to y=59.44 at the right edge) — the reported "black dot" was the intended silhouette, not a bug. Tab path restored byte-for-byte to the Figma export; body + pocket were already exact. Pressed export (layers +22) mirrors the app's existing `y: 22` press animation, no other changes. Red pending-dot removal still stands. Byte-verified against `mobile-folder-idle.svg`; `tsc --noEmit` green; deployed 2026-09-09.

### 2026-09-08 - instant event navigation (native full prefetch)

- **Full prefetch on all event-detail links:** `EventCard`, `FolderCard`, `EventListItem`, `ArchiveEventRow` now render `prefetch` on their `Link`/`MotionLink` — the full dynamic event route is warmed by Next's built-in viewport-first scheduler (visible links first, hover/touch intent next, newer replaces older, off-screen discarded). No custom IntersectionObserver. Receipt images are still NOT prefetched (RSC prefetch transfers no `<img>` bytes; they load progressively after render). `NavItem` + Home View-all links keep the 09-07 intent-only behavior. Supersedes the 09-07 "event/folder links never passively prefetch" stance for these four primitives — that fix targeted loading-shell-only prefetch + doubled CSS-hidden-trees requests; full-RSC prefetch + viewport batching + the 5-min snapshot below is the approved replacement.
- **5-minute snapshot (`next.config.ts`):** `staleTimes.static` 30s → 300s (Next default) — applies to fully prefetched routes only; `dynamic` stays 30s. Server still runs `requireRole` + RLS on every prefetch RSC; cache is browser-memory only, never crosses users/reloads/logout. No server cache, no new API, no storage: it is the 5-min client snapshot the user approved.
- **One silent reconcile in `EventLiveRefresh`:** fires `router.refresh()` once ~500ms after mount (ref guard `silentTimer`/`silentFired` + cleared on unmount) — a page served from the prefetch snapshot re-fetches the authenticated RSC in place (no skeleton; client state + scroll preserved) and realtime stays authoritative afterwards. Existing 2s throttle merges near-simultaneous realtime refreshes; no loop by construction. Verification: local `next build` green; **production-verified 2026-09-08 on `liquifi-app.vercel.app`** — Home load prefetched all 4 visible event routes (full RSC, not shells), one silent `router.refresh()` per mount (home + event page), a folder click rendered from cache with **zero page-network requests and no skeleton**, no further requests after 4s idle (no loop), zero console errors.

### Bottom-sheet gesture suite (2026-09-07 — final state)

- **Summary:** `CssBottomSheet` + `lib/bottom-sheet-drag.ts` — 3-snap native pointer-event sheet (no Framer). Grabs only at content-top; dismiss via top-band fling or close button; `MIN_SHEET_DRAG = 4px`; momentum + elastic top pull (`TOP_PULL_BAND = 48`). `max-h-[85dvh]` on all 7 sheets (Chrome Android `vh` bug). Dots rail removed; MID reachable by manual drag. Full gesture spec lives in `ui-registry.md` §Cross-Cutting Patterns.
- **Superseded chain:** framer native → `use-drag-to-dismiss.ts` v1–v4 → native inline → 3-snap port → elastic top → momentum → final (see `ui-registry.md` for user reports that drove each iteration).

### 2026-09-07 — misc

- **FadeIn containing-block fix (`fde204e`):** `useFadeIn` returns `transform: "none"` once visible (not `translateY(0)`) — the identity transform established a fixed-position containing block, so any `position: fixed` modal inside it (e.g. View Report "No report yet" dialog) positioned against the wrapper. `none` is not a containing block.
- **Entrance animations:** switched from framer-motion to React/CSS `components/ui/FadeIn.tsx` (`useFadeIn(delayMs)` hook co-located; reduced-motion `matchMedia` guard returns `{}`; plain div + inline styles). `EventPageEntrance.tsx` deleted. `lib/motion-variants.ts` kept for admin `DepartmentsListClient` + dialog/sheet springs (staggerContainer staggerChildren 0.04/delayChildren 0.03, fadeUpItem y:10 0.28s tween `[0.16,1,0.3,1]`). Stagger via `delay={30 + index * 80}`.

### 2026-09-07 — performance pass (navigation responsiveness)

- **Production navigation correction (verified 2026-09-07):** the prior local result did not represent the deployed Vercel bundle (the work remains uncommitted). A production trace found Home LCP **5.06s**, event-page LCP **7.12s**, and a folder navigation whose full RSC payload took **3.31s** after a 332ms loading-shell first byte. InsForge database queries themselves were 2–32ms; the issue was unnecessary cross-network RSC work: every visible event card auto-prefetched only its loading shell, and CSS-mounted desktop + mobile variants doubled those requests. `EventCard`, `FolderCard`, and `NavItem` now begin with `prefetch={false}` and enable full Next prefetch only on hover/focus/touch intent; Home's raw View-all links are explicitly disabled. Three event-detail `loading.tsx` files share `EventPageLoading`, replacing the generic busy fallback immediately on folder clicks. `EntryCard` receipt `<img>` requests are now low priority + async decode; the production trace identified ~979KB avoidable receipt transfer as event-page LCP pressure. `EventLiveRefresh` suppresses expected connect-cancel errors after unmount. **Verification:** production-build Home issued no event-detail prefetches; hovering one event issued only that event's prefetch; direct navigation displayed `Loading event` immediately; `tsc`, production build, and scoped ESLint had zero errors (five pre-existing warnings in `EntryCard`/`EventCard`). Files: `components/{layout/NavItem,events/{EventCard,FolderCard,EventPageLoading,EventLiveRefresh},entries/EntryCard}.tsx`, role event `loading.tsx`, `app/treasurer/home/client.tsx`.

- **Client cache re-enabled (`next.config.ts`):** `experimental: { staleTimes: { dynamic: 30 } }`. Dynamic pages (every dashboard) defaulted to 0s — each click re-ran every DB query (the persistent "loads first every time I open a page" feel). 30s now applies to the CLIENT router cache only (repeat navigation renders instantly, no DB hit). `router.refresh()` after every mutation, `force-dynamic`, and hard reloads all bypass it — same-device freshness unchanged. RSC payloads replace whole pages, never merge, so no stale-data composition risk. Shared layouts are NOT refetched on navigation (documented partial-rendering behavior).
- **`getEventDashboard` parallelized (`lib/queries/events.ts`):** same 5 queries in 2 `Promise.all` batches (event + entries + report-lock; then voided-name + creator lookups). Sequential per-name resolve blocks removed; the redundant second report-lock query (duplicate of batch 1's) deleted.
- **Page-level `Promise.all`** for paired dashboard fetches in: `app/treasurer/events/[eventId]` (dash + latestReport), `app/treasurer/reports/[eventId]` (dash + all reports), `app/adviser/events/[eventId]`, `app/adviser/reports/[eventId]`, `app/admin/departments/[deptId]/reports/[eventId]`. Trade-off accepted: a `notFound()` between the two now discards one parallel query (404s are rare).
- **`AdviserReportReview` optimistic flash:** `displayStatus = flash ?? report.status` — after confirm, approve/reject flips the header badge and hides the action section instantly; `setFlash(null)` + error restore them on failure. Presentation-only, the POST itself is unchanged; the server stays the source of truth (no state-machine shortcut).
- **Cross-device live refresh — backend objects LIVE on prod (applied via `insforge_run-raw-sql`):** `realtime.channels` row `event:%`; SECURITY DEFINER `public.notify_event_changed()` (handles DELETE via `OLD.event_id`); AFTER INSERT/UPDATE/DELETE triggers `events_realtime_changed` / `reports_realtime_changed` on `entries`/`reports`. Payload is `'{}'::jsonb` — channels are readable by the anon key, so we broadcast only "something changed", never entry/report data. `DROP TRIGGER IF EXISTS` preceders make it reversible.
- **Frontend:** `components/events/EventLiveRefresh.tsx` (client, renders null) mounted ONCE in the treasurer event page — first consumer of `lib/insforge-client.ts`. Uses the SDK's Socket.IO realtime API (`insforge.realtime.connect/on/subscribe/off/disconnect`) per the `fetch-docs` real-time docs — note AGENTS.md's `channel.on("postgres_changes", ...)` example does NOT match this SDK (that's the Supabase API). 2s throttle via `useRef`; cleanup on unmount. **Known ceiling (ponytail marker in code):** subscription open to anon keys — safe (empty payload + authed refresh) but restrict via RLS on `realtime.channels` if auditing demands it. Only the treasurer event page subscribes; adviser/admin pages still ride the 30s window.

### Receipt parsing — final fast-path design (2026-09-05, user-verified closed)

- **Model is NOT the bottleneck:** `gemini-3.5-flash-lite` pinned since 08-02 (bench: ~2–3s at ANY resolution — prompt tokens pinned ~1065, Gemini resamples internally; at 1024 it GARBLES text, SVG source leaked). Root cause of the 18s was the pipeline: the route uploaded the image, inserted a provisional row, then `getReceiptBlob` downloaded it back just to build a data URL — storage round-trips on a cold Hobby function blew the 8s budget and the client retry doubled it.
- **`app/api/entries/receipt/route.ts` (final):** feed the already-client-compressed multipart `File` DIRECTLY to Gemini (data URL from `image.arrayBuffer()` — no storage read); Gemini + `requireRole` start concurrently; the blob upload runs after auth, overlapping the model call (still needed for the retry path + `image_url`); on success insert straight to `ai_parsed` (single insert, no provisional hop). Verdict → delete blob + 422 + audit; duplicate → delete blob + 409; transient Gemini failure → insert `pending_ai_parse` (blob already uploaded) + audit so the retry route works. Critical path ≈ 3–4s warm. **`app/api/entries/receipt/[entryId]/route.ts` is the retry endpoint** — on a transient `{status:"parsing"}` the client re-parses the SAME provisional row from its stored blob (no re-upload, no duplicate blob).
- **Constants (all still in place):** `lib/gemini.ts` `TIMEOUT_MS = 8_000` (Vercel Hobby has a hard 10s function ceiling — a 9s timeout risks the kill; room must stay for cold-start). `lib/image.ts` `MAX_DIM = 1600`, JPEG q `0.8` (legibility; the 1280/0.7 "fewer tokens = faster" theory was measured wrong and reverted). `MAX_ATTEMPTS = 2` (primary + 1 reparse). `agent/receipt-parser.ts`: transport/timeout errors (`GeminiError`, `TimeoutError`, `AbortError`, `/aborted|timeout|timed\s?out/i` via `isTimeoutError`) are NON-retryable — throw immediately; the 3-attempt budget is preserved ONLY for fast zod/schema-mismatch re-parses (model already returned).
- **Orphan leak fixed (09-04):** verdict-422 AND duplicate-409 branches must delete the provisional row — shared `discardProvisional(insforge, entryId, imageUrl)` helper (DB delete that THROWS on failure so it can't silently orphan, + best-effort blob delete). A failed parse never leaves an untitled ₱0.00 `pending_ai_parse` row.
- **[SUPERSEDED]** 09-04 two-phase upload (202 + poll, `POLL_DELAY_MS=1500`) — shipped, then killed: it regressed perceived time to ~20s (3 attempts × Gemini + sleeps). The single-request fast path above supersedes it. Deploy note for the two-phase era is in Notes.

### Classification (guided upload) — rules that never change

- Outcomes: `valid` / `borderline` / `invalid` / `multiple`. Doubt → `borderline`, never `invalid`. Verdicts short-circuit with guidance + audit, NO Entry row, and never count toward the 3-attempt ceiling. Only `valid` creates a row (at `ai_parsed`). `multiple` (2+ distinct documents, even partially visible; hand/fingers ≠ second document; doubt resolves by document count) → 422 `entry.receipt_multiple_documents`, "One receipt per upload" banner + mobile hint.
- `document_type_raw` = verbatim printed label, never forced into an enum; `""` when none (handwritten). `document_number` tied to that label; `""` when no label-tied number — never null, never made up (numberless receipts skip the duplicate check). `amount` = Final Amount Due, never sub-total. `item_breakdown` required. Category inferred at parse time (7-way: transportation/meals/honorarium/supplies/printing/rental/others; doubt → `others`); AI final, review read-only, no backfill.
- Duplicate check: reject if `(document_type_raw + document_number)` already exists in the same event.
- Upload pipeline: client-side downscale via shared `lib/image.ts` (≤1600px longest side → JPEG q0.8, `createImageBitmap` with `imageOrientation: "from-image"`, white-composited for PNG transparency, HEIC/HEIF + <1MB pass through untouched, any failure returns the original — downscale is an optimization, never a blocker). Database `entries.status` IS CHECK-constrained (see Notes — AGENTS.md claim of free text is wrong).

### Budget & entries — money laws (as of 08-30, all still true)

- **Budget immutability:** `budget_locked` derived from any entry row existing (statuses irrelevant). Original arbitrary-edit path (`updateEventBudget` / `EditBudgetModal`) deleted — budget increases now go through verified proof uploads via `IncreaseBudgetModal` + `POST /api/proofs`. Gate: increase allowed only while `isLocked` is false (no pending/approved report). Archive locks still handled upstream by `canMutate`.
- **Overspend gate is pending-aware + fires once per event:** committed = `deducted + pending_approval` (`.in("status",[...])`), so stacking pending manual entries re-prompts the Step-18 `overspendRequired` reason before insert. `causes_overspend=true` is a durable flag — `rejectEntry` doesn't clear it; `alreadyFlagged` makes the prompt fire at most once per event. Receipt `confirmReceiptEntry` gate untouched (receipts deduct immediately — no pending). Approval re-derivation (`actions/approvals.ts`) re-derives the TRUE deducted-only crossing and clears stale flags (pending-side flag is provisional until deduction).
- **Withdraw/discard semantics (08-05 final; `discarded` no longer reachable):** `ai_parsed` → Discard = hard delete; `pending_approval` manual → Withdraw = hard delete; `rejected`/`resubmitted` → permanent (resubmit only; 2nd rejection terminal); `deducted` → Void. `discardReceiptEntry` is server-guarded and also guards `pending_ai_parse` (all entry mutations block during pending/approved reports — `is_locked` parity). `treasurer_reviewed` enum member exists but is unused (authoritative transition is `ai_parsed → deducted`).
- **Void** lives in the entry info modal (full-width destructive pill when `canMutate && status === "deducted"`).
- **Manual entries (Step 16):** rows `type:"manual"`, `status:"pending_approval"`, never deducted at creation. Per-category gate `max(minCeiling, round(budget×pct/100))`; explanation never blocks. **Floors:** Transportation 20%/₱300 · Meals 55%/₱500 · Honorarium 45%/₱1,000 · Supplies 25%/₱500 · Printing 12%/₱300 · Rental 65%/₱1,000 · Others 20%/₱300. Manual photos: up to 4 (`MAX_PHOTOS`/`MAX_IMAGES_PER_ENTRY=4` — raise client+server together); `image_url` = JSON array string when N>1, bare key when N=1 (text column, legacy-safe); keys parsed via `lib/image-keys.ts` (web-safe — `lib/storage.ts` imports the server SDK); photo transport is `POST /api/entries/manual/photo` (FormData, `requireRole("treasurer")` + own-dept + event `open` + not report-locked, mime/size validated) — NEVER a `File` inside a server action payload (`submitManualEntry` takes `entryId` + `imageKey`, not `photoFile`); invariant: failed upload ⇒ no row.
- **Currency/number inputs:** manual forms store raw strings while typing, coerce at edges only (`toNumber` comma-aware everywhere); live separators (reversal of an earlier tradeoff); count inputs use `FloatingInput.digits` + `inputMode="numeric"` (`type="number"` allows `e`/`E` → NaN→0; no spin buttons).
- **PostgREST gotchas (08-08):** null filters need `.is(col, null)`, NOT `.eq(col, null)` (400); to-one embeds return a single object — extract shape-agnostically; `.in()` with an empty array errors — guard it; `{ data }` without `error` silently degrades and skips logic — never trust an unchecked count.
- Adviser batch-approve dispatches `executeBatchApprove` (the dialog once dispatched `executeUserAction`, which no-ops without `userId`). Rejection reason threads to `EntryDetailModal` red callout. `entryTitle()` = supplier → "Category — detail" → category → "Untitled entry".

### Reports (Phase 7–9) — rules that never change

- **Reports are never overwritten:** regeneration after rejection/cancellation creates a NEW row reusing the same `fs_document_number` with `revision_count` incremented (audit-only, never printed). `getAllReportsByEvent` = full history ordered `generated_at` desc; newest drives the precondition gate, "Previous revisions" card lists the rest.
- **FS number anchored per EVENT:** number = first-assigned `fs_document_number`; suffix increments per event via dept/year counter (`FS-{DEPTCODE}-{YYYY}-{00001}`); cancelled/rejected regenerations never consume a new number.
- **Lifecycle groups:** treasurer — `Needs attention` (no report OR newest `rejected`; newest `cancelled` collapses to "No report yet" via `getLatestReportsByEvent` dropping it to null; its PDF stays reachable on the detail page's Previous revisions) → `Pending approval` → `On file` (`approved`, event still open) → `Archived` (event `archived`, always carries an approved report; read-only). Adviser — `Pending approval` → `Reviewed` (`approved`/`rejected` trail) → `Archived`; excludes no-report events.
- **Cancel:** treasurer-only, dept-scoped, event not archived, `pending_adviser_approval` only, race-guarded (racing approve → 409); no adviser cancel notification. **Reject:** whole report re-visible + re-generable; rejected PDF stays listed.
- **PDF UX:** reports are downloadable documents, not inline renders — `PdfViewer.tsx` deleted (pdfjs-dist NOT Node-safe — `DOMMatrix` at module scope crashes SSR on static import; dynamic `await import` inside the render effect + `GlobalWorkerOptions.workerSrc` from a module constant was the fix; pdfjs-dist left in `package.json` unused to avoid churn). `ReportFileCard.tsx` (server component) everywhere: View = `?inline` (default) / Download = `?dl=1` → `Content-Disposition: attachment` via `app/api/reports/[reportId]/pdf/route.ts`. `pdf_url` stores the storage KEY, never a browser-loadable URL — proxy streams. `reports` orders by `generated_at` (no `created_at`).
- **Generation (Step 20) discoveries:** no `router.refresh()` after generate (kills client preview); cross-user notification inserts silently failed → added `notifications_same_dept_insert` RLS policy. Signatories client-only (localStorage) until the report is persisted. Signatory columns wrap at 3 per row (`flexWrap:"wrap"` + `width:"33.33%"`). Note: codebase uses `insforge.database.from(...)`.
- **Archive (Step 25):** `requireRole("treasurer")`; guards → 404 (no event / wrong dept), 403 (no latest approved report), 409 (already archived or `has_unresolved_overspend`); uploads each signed page (`uploadSignedReport(report.id, i+1, file)`), `countPdfPages` = regex on `/Type /Pages … /Count N` (fallback 1 — pdfjs-dist client-only), then `verifySignedDocument` (agent/document-verifier.ts: gpt-4o via OpenRouter, 3 attempts; checks fs_document_number + signature-like marks per `report_signatories` ordered by `sort_order` + page count — **completeness only, never authenticity**); fail → best-effort delete uploaded keys + 422 with per-check `checks`/`summary`; pass → race-safe event update `.eq("id").eq("status","open")` (0 rows → 409 + rollback) + `signed_document_urls`/`signed_page_count`/`signing_confirmed_by|at` + audit `event.archived`. `canArchive = latestReport?.status === "approved" && !isArchived && !event.has_unresolved_overspend`.
- **@react-pdf/renderer:** server-side only, `renderToBuffer` only, in `app/api/reports/generate`; buffer → storage directly (never disk); supported CSS subset listed in AGENTS.md/library-docs.

### Notifications & Web Push

- **Single choke point:** `createNotification` in `lib/notifications.ts` (content map drives push + list pages; every type also calls `sendPushToUser`). Types: `report_ready_for_approval` (adviser, fires on EVERY regeneration, not just first), `report_rejected` (treasurer), `report_approved` (treasurer), `entry_rejected`, `entry_approved` (treasurer, to `entry.created_by`), `manual_entry_pending`, `event_overspend` (adviser, fires on the TRUE deducted crossing only — NOT the provisional pending flag — once per event via the durable `causes_overspend`), `adviser_signup_pending` (admin), `treasurer_signup_pending` (dept adviser), `signup_approved`/`signup_rejected` (applicant). Badge count passed server-side from layouts (refresh-on-navigation — deliberate deviation from a client-polling provider).
- **Web Push was never delivering (root cause fixed 08-30):** `push_subscriptions` was empty because nothing ever called `Notification.requestPermission()` — `PushSubscriber` bailed on `permission !== "granted"` and no prompt existed. Fix: `lib/push-client.ts` `ensurePushSubscription()` (get-or-create + sync) + `PushEnableToast.tsx` (first-login toast, once per browser via `liquifi_push_prompt_dismissed` localStorage, shown only when `permission === "default"`, Enable button = the user-gesture `requestPermission()` trigger; denied → settings link). Real delivery needs the user to click Enable + Chrome in background; **iOS Safari has no Web Push**.
- **Security hardening (08-31):** dropped the `push_diag` debug table (RLS-off, was exposing real push endpoints + emails + UUIDs to `anon`); removed `diag()` calls + temp test-push route; `REVOKE EXECUTE ... FROM PUBLIC/anon` on `get_user_push_subscriptions(uuid)` (was an IDOR) and `notify_signup_approvers(...)` (was a spoof vector), both re-granted to `authenticated` only. `purge_rejected_user(p_id)` SECURITY DEFINER: deletes notifications + push_subscriptions (NO ACTION FKs) then `auth.users` (cascades to `public.users` + `auth.user_providers`) — frees the email for a fresh signup; `audit_logs` has no FK so the `user.rejected` row survives; `signup_rejected` notification removed (target no longer exists); ACL `{authenticated, project_admin}`. **Flagged:** sweep remaining SECURITY DEFINER functions (`create_user_profile`, `purge_rejected_user`, `get_user_department_id`, `get_user_role`, signup hygiene) for PUBLIC EXECUTE + unguarded arbitrary-parameter reads.
- All push goes through `lib/web-push.ts`; on 410 Gone delete the subscription immediately; service worker `public/sw.js`.

### Signup & auth (final state after the 08-31/08-30/08-22 work)

- **Wizard:** 3 steps (Name → Account(email/password) → Council(role/dept)), forward-only (top arrow = back to `/login`), per-step gating via the `submitted` red-border pattern, `stepError` vs `apiError` distinct states. Step 2 `POST /api/auth/signup` = validation-only gate (required fields, IP rate limiter, `check_signup_email` → "Email already exists", client email regex `EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/` + 8-char password gate) — NO account creation, NO email. Step 3 `POST /api/auth/signup/complete` = the SINGLE point the account is born + the OTP email fires (re-checks same gates, purges any `in_progress` ghost, calls `signUp`, resolves/auto-seeds department). Step-aware back nav + reload-safe step index + per-email OTP cooldown survives back-nav (`signup_otp_sent` timestamp drives the `/otp` countdown). 409 "just started" dead-end fixed: re-proceed on an `in_progress` account routes to `/otp` (original OTP still valid — ghosts purge ~10min, OTP 15min; InsForge keeps ONE `email_otps` row per email+purpose, so no double-send).
- **Approver notifications fire post-OTP:** the `users` row only exists after `POST /api/auth/create-profile` runs `create_user_profile` — the old pre-OTP notify block could never find the adviser (`get_user_department_id()` → NULL). Notifications now run in `create-profile` AFTER the row exists: treasurer → dept's active adviser; adviser → all active admins.
- **Login lockout v2 (cycles):** `lib/rate-limit.ts` email bucket, 3-cycle state machine (`mode: "cycles"`): C1 = 5 fails → 1 min, C2 = 3 fails → 5 min, C3 = 3 fails → 24h soft lock (cooldown rides `IDLE_TTL_MS`; on lift the staleness check wipes the bucket same-request — no one-fail-from-relock zombie); cycle counters reset on each cooldown trip; stale buckets (>24h idle) restart at C1; success clears everything; `softLocked: true` JSON flag + inline login modal → `/forgot-password`. Redemption: `/api/auth/change-password` calls `clearRateLimit("e:{email}")` after successful `resetPassword` (safe — requires OTP + inbox). IP bucket stays the flat ladder: `baseMs × 2^(failures − threshold)`, cap 30min email / 1hr IP, threshold email 5 / IP 15 (IP key = `xff first hop ?? x-real-ip`; bucket skipped entirely when neither header exists); 429 + `Retry-After` before InsForge is touched; success clears only the email bucket (typos forgiven); 403s (pending/rejected accounts) never count; blocked requests never extend timers. In-memory ceiling accepted: resets on deploy, per-instance — a DB-backed table is the documented upgrade path. Signup IP bucket: `SIGNUP_IP_LIMIT`, threshold 10/min, cap 15 min, cleared wholesale on successful creation.
- **Session:** `proxy.ts` (Next 16) — `updateSession` runs FIRST so redirect decisions use the real post-refresh token; refresh cookie `insforge_refresh_token` = 2 days (`REFRESH_COOKIE_MAX_AGE`, Max-Age beats Expires per RFC 6265), re-pinned on every navigation via `updateSession` → sliding window; `user_role` cookie 7d (cosmetic redirect hint must outlive the window); away >2 days → re-login (one constant). Browser client auto-refresh via `POST /api/auth/refresh` (`createRefreshAuthRouter`). OTP verify manually persists access/refresh cookies (SSR client never writes response cookies). `middleware.ts` is deprecated vs Next 16 `proxy.ts`.
- **Foundational (07-17/07-18):** OTP 10-min expiry, resend 60s, max 5/hour, 5 wrong attempts lock. Verified OTP → stays `pending_approval` → `/pending-approval`. Admin accounts never via `/signup` (role selector excludes Admin client-side AND server-side). `api/auth/status` polling (10s) → redirect/banner + nodemailer welcome email on approve. Dept auto-seeds on first signup. `/forgot-password → /otp?purpose=reset → /change-password → /login`.
- **Optimistic UI (08-31):** admin + adviser approve/reject are instant — remove the row on click, run the server action async in the background, roll back on failure, `router.refresh()` on success. `window.confirm` REMOVED (the "dead air"; an accidental Reject now purges — rollback only guards server preconditions). Double-click guards: `pendingRef: Set<string>` (admin), `userActionRef: Promise<void>` (adviser). Batch approve keeps its spinner (bulk, still awaited). **ponytail:** if a confirm is ever wanted again it must be an app-owned non-blocking dialog, never `window.confirm`.

### Admin / audit

- **Audit Log (Phase 11 start, 09-01 readable rewrite):** `lib/audit-log-view.ts` maps every `action` + `metadata_json` to `{ label, category, icon, tone, summary, details }` — 23 mapped actions, human labels + one-line summaries (amount in PHP, document #, FS number, from/to budget, rejection reason, anchor), NO raw JSON in the UI; unknown codes fall back gracefully. Filter bar: Who (actor, `actor_role` resolved), Category, From/To dates, Clear — client-side over the `.limit(100)` fetch (server-side pagination is the upgrade path). Page fetches `audit_logs` scoped to dept, desc, actor names via a second query on distinct ids.
- **Departments:** one active adviser + one active treasurer per dept (partial unique indexes on users). Roles: admin (department_id null, unrestricted), adviser, treasurer. Admin RLS: admin-unrestricted on dept-scoped tables — proven by existing admin reads.
- **FolderCard (09-02):** Figma two-tone folder ported with token fills only — `fill-neutral` rim + `fill-border-strong` body (no hardcoded hex, per ui-tokens).

### Established conventions (hold for new work)

- **UI patterns, component specs, and class conventions** → see `ui-registry.md` (do not duplicate here).
- **Auth guard:** `lib/auth-guard.ts` `requireRole(role, departmentId?, precondition?)` is THE server-side security boundary (built from `docs/auth-matrix.md`). Role checks in route-group layouts are server-side; navigation visibility is cosmetic only.
- **Imports:** `@/` maps to project root, never relative up more than one level. Storage access ONLY through `lib/storage.ts` (`upsert: false` everywhere). No `URL.createObjectURL` server-side. No `vercel alias set` anymore — see Deploy discipline in Notes.

---

## Notes

*(Environment quirks, sandbox limitations, verification commands, and anything a future agent needs before touching this codebase. Append, don't overwrite.)*

- **Schema vs docs:** `entries.status` IS CHECK-constrained despite AGENTS.md claiming free text (deployed two-phase upload hit `23514 ... entries_status_check`). `pending_ai_parse` was added to the allowed set. `entries.type` is also CHECK-constrained to `receipt|manual`. `get-table-schema` MCP does NOT surface CHECK constraints (only columns/indexes/FKs/RLS) — read `pg_constraint`. **Treat the CHECK constraints as authoritative; AGENTS.md status claim is wrong.**
- **SDK import:** server client is `@insforge/sdk/ssr` (`createServerClient`); there is no standalone `@insforge/ssr` package. `lib/insforge-server.ts` is correct. (architecture.md's import path was corrected.)
- **Storage RLS:** InsForge manages storage access internally — no SQL `storage.objects` policies. Buckets are created public; access control is enforced APPLICATION-side via path conventions (`receipts/{deptId}/events/{eventId}/receipts/{entryId}.jpg`, `signed-reports/{deptId}/reports/{reportId}/page-{n}.jpg`, `avatars/{userId}.jpg`) + server auth guards. `image_url` = storage key (never a browser URL; `GET /api/entries/[entryId]/image?i=N` streams with `requireRole`).
- **Deploy discipline (2026-09-07 clean-slate):** `commit → git push origin master → Vercel auto-deploys` to `liquifi-app.vercel.app` (the project IS named `liquifi-app`, so that URL is its default production domain and auto-tracks every push — the old manual-alias foot-gun is gone BY CONSTRUCTION). Never revert to `vercel alias set` for the canonical domain. Emergency hotfix still possible via local `vercel --prod` (`.vercel/` is re-linked). Production branch = `master` = repo default. Before the clean-slate, `liquifi-app.vercel.app` was a MANUAL alias that did not follow deploys — that history is irrelevant now.
- **[SUPERSEDED deploy note, kept for context]** The 09-04 two-phase receipt upload was deployed to prod, then superseded the same week by the single-request fast path (deployed). The single-request design is CURRENT; the two-phase only exists as the demoted `[entryId]` retry handler.
- **Verification patterns that work:** real touch = CDP `Emulation.setTouchEmulationEnabled` + `Input.dispatchTouchEvent` only; synthetic PointerEvents can't reproduce gesture bugs. `tsc --noEmit` + `next build` green is the bar; scoped eslint for touched files. Deterministic check scripts live in `scripts/` (`check-bottom-sheet-drag.ts`, `check-overspend-gate.ts`).
- **`users`-table lookup in `getCurrentUser()`** (auth-guard) requires the Phase-1 `users` table to exist before runtime auth works — structurally correct, live once schema + signup land (historical note).
- **Notification edge:** `report_rejected` fires only when the dept has an `active` treasurer — dev data may have none (and `users` reads are RLS-hidden to advisers) so it skips; guard clause, not a bug.

---

## How to Update This File

1. Keep **Current Status** honest: update Last completed / Next / open items as you finish things.
2. Add one dated bullet under **Decisions Made During Build** (newest first) describing what was actually built — file paths, exact function/table names, any deviation from `build-plan.md` or `architecture.md` and why. If it supersedes an earlier decision, mark the old entry `[SUPERSEDED]` and link to the new one.
3. Add to **Notes** only if something environment/verification-related happened (build failures, sandbox network issues, deploy quirks).
4. Keep history — condense verbatim detail on request, never silently drop a rule that still governs behavior. Full verbatim history lives in git.
5. If a decision here ever contradicts `architecture.md`, the architecture doc wins — flag the conflict in Notes rather than silently resolving it.
